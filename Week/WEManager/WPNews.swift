
import Foundation

import UIKit
import SocketRocket

class WPNews: NSObject {
private var gifPadding: Float = 0.0
var removeHeadString: String?



    static let shared = WPNews()
    
    var webSocket: SRWebSocket?
    var connectFailedCallBlock: ((Error) -> Void)?
    var connectSuccessCallBlock: (() -> Void)?
    var didReceiveMessageCallBlock: ((String) -> Void)?

@discardableResult
 func aidUniqueNetworkItemGraphicsEmpty(imageStyles: Int, lishiQueue: [Any]!) -> [String: Any]! {
    var screenC: String! = String(cString: [98,117,99,107,101,116,115,0], encoding: .utf8)!
    var filev: Int = 0
   for _ in 0 ..< 3 {
       var bodyC: [Any]! = [736, 903]
       var handingN: String! = String(cString: [111,118,97,108,0], encoding: .utf8)!
       var lineO: Float = 1.0
       var reusabled: String! = String(cString: [107,105,99,107,0], encoding: .utf8)!
         handingN.append("\(reusabled.count | Int(lineO))")
         handingN.append("\(bodyC.count)")
         lineO -= Float(reusabled.count)
      if 3 >= (reusabled.count + 4) || (Int(lineO) / 4) >= 2 {
         lineO += Float(bodyC.count)
      }
       var sortX: Bool = false
       var sharedj: Bool = false
         sortX = bodyC.count <= handingN.count
         bodyC.append(handingN.count + 2)
      while (!sortX && 2.96 > (lineO * 2.41)) {
         lineO -= Float(reusabled.count)
         break
      }
      if 4 <= reusabled.count {
          var systemo: Double = 1.0
          var first7: String! = String(cString: [116,121,112,0], encoding: .utf8)!
         sortX = handingN == (String(cString:[106,0], encoding: .utf8)!)
         systemo -= Double(Int(systemo))
         first7 = "\(Int(systemo))"
      }
         bodyC = [((String(cString:[100,0], encoding: .utf8)!) == handingN ? handingN.count : Int(lineO))]
       var isbdingI: [String: Any]! = [String(cString: [115,101,101,110,0], encoding: .utf8)!:580, String(cString: [115,110,97,112,0], encoding: .utf8)!:160]
       var againZ: [String: Any]! = [String(cString: [109,111,99,107,0], encoding: .utf8)!:[String(cString: [115,97,110,105,116,105,122,101,0], encoding: .utf8)!:String(cString: [114,101,100,114,97,119,0], encoding: .utf8)!, String(cString: [97,115,115,111,99,105,97,116,105,111,110,115,0], encoding: .utf8)!:String(cString: [105,115,115,117,101,0], encoding: .utf8)!]]
      if (againZ.count % 4) > 1 && againZ.count > 4 {
         againZ = [reusabled: 2]
      }
      filev ^= 1
   }
   for _ in 0 ..< 1 {
       var remarku: String! = String(cString: [99,111,110,99,97,116,100,101,99,0], encoding: .utf8)!
       var sourcep: Double = 2.0
       var logov: Double = 1.0
         remarku.append("\(remarku.count + 3)")
          var loginE: String! = String(cString: [101,120,112,114,101,115,115,105,111,110,115,0], encoding: .utf8)!
          var recordsF: Double = 3.0
          var sublyoutn: String! = String(cString: [109,118,112,114,111,98,115,0], encoding: .utf8)!
         sourcep /= Swift.max(3, Double(3))
         loginE.append("\(((String(cString:[68,0], encoding: .utf8)!) == loginE ? Int(recordsF) : loginE.count))")
         recordsF -= Double(Int(recordsF))
         sublyoutn.append("\(loginE.count % 1)")
       var socketk: String! = String(cString: [97,100,97,112,116,105,118,101,0], encoding: .utf8)!
       var l_widthZ: String! = String(cString: [110,115,117,112,112,111,114,116,101,100,0], encoding: .utf8)!
      repeat {
         sourcep *= Double(Int(logov))
         if sourcep == 2420347.0 {
            break
         }
      } while (sourcep < Double(l_widthZ.count)) && (sourcep == 2420347.0)
          var barA: Int = 0
         sourcep += (Double(socketk == (String(cString:[99,0], encoding: .utf8)!) ? socketk.count : Int(sourcep)))
         barA += barA ^ 3
       var imgc: Double = 5.0
       var common2: Double = 0.0
         imgc += (Double(remarku == (String(cString:[90,0], encoding: .utf8)!) ? Int(common2) : remarku.count))
         common2 += Double(3 ^ Int(logov))
      if l_widthZ.hasSuffix("\(sourcep)") {
         sourcep *= Double(Int(logov) ^ 3)
      }
      screenC.append("\(Int(logov))")
   }
   repeat {
      screenC.append("\(filev >> (Swift.min(screenC.count, 1)))")
      if (String(cString:[49,48,54,0], encoding: .utf8)!) == screenC {
         break
      }
   } while ((String(cString:[49,48,54,0], encoding: .utf8)!) == screenC) && (5 == screenC.count)
     let itemsRead: String! = String(cString: [105,100,101,110,116,105,99,97,108,0], encoding: .utf8)!
     var sourceGoods: Double = 9262.0
     let timelabelImg: Double = 1838.0
    var xvidLonMid :[String: Any]! = [String: Any]()
    xvidLonMid.updateValue(itemsRead.uppercased(), forKey:String(cString: [54,0], encoding: .utf8)!)
    sourceGoods += 43
    xvidLonMid.updateValue(sourceGoods, forKey:String(cString: [79,0], encoding: .utf8)!)
    xvidLonMid.updateValue(timelabelImg, forKey:String(cString: [117,0], encoding: .utf8)!)

    return xvidLonMid

}





    
    func disconnect() {

             aidUniqueNetworkItemGraphicsEmpty(imageStyles:5157, lishiQueue:[237, 22])


       var headerX: Double = 1.0
    var row4: String! = String(cString: [109,101,109,99,109,112,0], encoding: .utf8)!
   repeat {
      row4 = "\(Int(headerX) | row4.count)"
      if row4.count == 3632991 {
         break
      }
   } while (1.77 < (2.76 - headerX)) && (row4.count == 3632991)
   repeat {
      row4.append("\(1 & Int(headerX))")
      if row4.count == 866544 {
         break
      }
   } while ((Int(headerX) * 4) == 3 || (1.62 * headerX) == 1.57) && (row4.count == 866544)

   for _ in 0 ..< 2 {
      row4.append("\(row4.count * Int(headerX))")
   }
        webSocket?.close()
    }

@discardableResult
 func convertRecordDelay() -> [String: Any]! {
    var logod: String! = String(cString: [103,101,115,116,117,114,101,0], encoding: .utf8)!
    var successV: String! = String(cString: [115,112,101,101,99,104,0], encoding: .utf8)!
   while (2 <= successV.count) {
       var time_zV: Double = 1.0
       var recognizedS: Double = 0.0
       var p_heightw: [Any]! = [9726.0]
         time_zV += Double(p_heightw.count | 1)
       var dismiss7: Bool = true
      repeat {
          var iost: Bool = false
          var detaillabelC: [String: Any]! = [String(cString: [97,116,111,109,0], encoding: .utf8)!:String(cString: [103,101,110,99,98,0], encoding: .utf8)!, String(cString: [99,111,109,112,101,110,115,97,116,101,0], encoding: .utf8)!:String(cString: [109,97,99,114,111,115,0], encoding: .utf8)!]
          var clearE: Bool = true
         recognizedS += (Double((dismiss7 ? 1 : 1) * p_heightw.count))
         iost = !iost
         detaillabelC = ["\(clearE)": ((clearE ? 3 : 5))]
         if 367533.0 == recognizedS {
            break
         }
      } while ((time_zV + 1.21) > 2.12 || 4.49 > (recognizedS * 1.21)) && (367533.0 == recognizedS)
         p_heightw = [Int(recognizedS) << (Swift.min(1, labs(Int(time_zV))))]
       var d_object_: Float = 3.0
      if dismiss7 {
          var time_0qG: String! = String(cString: [97,117,116,111,99,111,114,114,0], encoding: .utf8)!
          var lishi5: [Any]! = [318, 932, 952]
          var class_1S: Float = 1.0
          var present6: Float = 0.0
          var codev: Bool = false
         dismiss7 = !dismiss7
         time_0qG = "\(1)"
         lishi5.append(3)
         class_1S /= Swift.max(2, Float(3))
         present6 /= Swift.max((Float((codev ? 5 : 4) % (Swift.max(time_0qG.count, 5)))), 1)
         codev = !codev && lishi5.count <= 31
      }
      if (time_zV * recognizedS) < 4.65 && (4.65 * recognizedS) < 3.48 {
         time_zV += (Double((dismiss7 ? 1 : 2) & 2))
      }
          var relationa: String! = String(cString: [97,116,111,98,111,111,108,0], encoding: .utf8)!
         d_object_ /= Swift.max(5, Float(Int(recognizedS)))
         relationa.append("\(3 + relationa.count)")
         dismiss7 = (p_heightw.contains { $0 as? Double == recognizedS })
      logod = "\(successV.count)"
      break
   }
      successV = "\(successV.count)"
   for _ in 0 ..< 2 {
       var sumW: Double = 1.0
       var refresht: String! = String(cString: [102,114,101,101,100,0], encoding: .utf8)!
       var modityb: Bool = true
       var while_zE: Double = 1.0
         while_zE += Double(Int(sumW) >> (Swift.min(2, labs(3))))
      while (3.51 < while_zE) {
         modityb = refresht.hasPrefix("\(sumW)")
         break
      }
      for _ in 0 ..< 2 {
         sumW /= Swift.max(1, (Double((modityb ? 5 : 5) * Int(while_zE))))
      }
         while_zE /= Swift.max(Double(refresht.count), 1)
          var a_managerj: String! = String(cString: [101,110,103,108,105,115,104,0], encoding: .utf8)!
          var window_ko: Int = 2
         refresht = "\(Int(while_zE) & 1)"
         a_managerj = "\(window_ko)"
         window_ko |= a_managerj.count
      repeat {
          var login4: String! = String(cString: [99,101,114,116,105,102,105,99,97,116,101,115,0], encoding: .utf8)!
          var jsonv: String! = String(cString: [109,117,115,120,0], encoding: .utf8)!
          var sourceR: [Any]! = [944, 303]
          var candidateu: [String: Any]! = [String(cString: [117,115,101,114,110,97,109,101,0], encoding: .utf8)!:String(cString: [98,105,110,116,114,101,101,0], encoding: .utf8)!]
          var userK: Bool = true
         modityb = 75 > candidateu.count
         login4 = "\(((String(cString:[51,0], encoding: .utf8)!) == jsonv ? (userK ? 3 : 4) : jsonv.count))"
         sourceR = [jsonv.count - login4.count]
         candidateu["\(userK)"] = sourceR.count
         if modityb ? !modityb : modityb {
            break
         }
      } while (3.75 > (while_zE + 2.80) && modityb) && (modityb ? !modityb : modityb)
         refresht = "\((Int(sumW) << (Swift.min(4, labs((modityb ? 4 : 3))))))"
      if while_zE > 1.39 || (while_zE + 1.39) > 5.16 {
         while_zE -= (Double((String(cString:[86,0], encoding: .utf8)!) == refresht ? (modityb ? 4 : 5) : refresht.count))
      }
         modityb = 70.100 <= while_zE || modityb
         sumW /= Swift.max(4, (Double((modityb ? 1 : 3))))
      while (refresht.count < 3 || modityb) {
          var tabbarf: [Any]! = [String(cString: [115,97,109,112,108,101,115,0], encoding: .utf8)!, String(cString: [99,97,118,108,99,0], encoding: .utf8)!, String(cString: [98,110,98,105,110,0], encoding: .utf8)!]
          var levelE: [Any]! = [String(cString: [115,101,110,100,116,111,0], encoding: .utf8)!, String(cString: [105,99,111,110,105,99,0], encoding: .utf8)!]
          var navgationm: [String: Any]! = [String(cString: [111,112,116,105,109,105,122,101,0], encoding: .utf8)!:429, String(cString: [105,110,116,101,114,108,101,97,118,105,110,103,0], encoding: .utf8)!:547, String(cString: [98,101,99,111,109,101,0], encoding: .utf8)!:23]
          var alamofirek: Int = 4
         modityb = !modityb
         tabbarf.append(navgationm.values.count << (Swift.min(labs(1), 3)))
         levelE.append(levelE.count & navgationm.count)
         alamofirek <<= Swift.min(labs(alamofirek & 2), 4)
         break
      }
      if 3 < refresht.count || modityb {
         modityb = sumW >= 46.99 || modityb
      }
      logod.append("\(2 >> (Swift.min(5, labs(Int(while_zE)))))")
   }
      successV.append("\(logod.count | 2)")
     var logoAida: String! = String(cString: [114,103,98,110,0], encoding: .utf8)!
     var modityPicture: Int = 5920
    var imcdataPlayLuma :[String: Any]! = [String: Any]()
    imcdataPlayLuma.updateValue(logoAida.lowercased(), forKey:String(cString: [50,0], encoding: .utf8)!)
    modityPicture += 50
    imcdataPlayLuma.updateValue(modityPicture, forKey:String(cString: [71,0], encoding: .utf8)!)

    return imcdataPlayLuma

}





    
    func connect(scoketlink urlStr: String) {

             convertRecordDelay()


       var edit4: Double = 1.0
    var addressw: Bool = true
    var update__cM: String! = String(cString: [98,108,97,99,107,115,0], encoding: .utf8)!
   for _ in 0 ..< 2 {
      edit4 -= (Double((addressw ? 5 : 1)))
   }
   if (5.3 * edit4) > 4.1 || !addressw {
      edit4 /= Swift.max(Double(update__cM.count / 1), 5)
   }

      addressw = update__cM == (String(cString:[99,0], encoding: .utf8)!) || edit4 < 61.91
   repeat {
      addressw = !addressw
      if addressw ? !addressw : addressw {
         break
      }
   } while (addressw ? !addressw : addressw) && (!update__cM.hasSuffix("\(addressw)"))
        webSocket = SRWebSocket(url: NSURL(string: urlStr)! as URL)
   repeat {
      addressw = edit4 <= 13.66 || addressw
      if addressw ? !addressw : addressw {
         break
      }
   } while (1.73 > edit4 || 3.79 > (edit4 + 1.73)) && (addressw ? !addressw : addressw)
        webSocket?.delegate = self
        webSocket?.open()
        
    }
    
}

extension WPNews: SRWebSocketDelegate {

@discardableResult
 func unsafeIncludeDisappear(applyNick: Double, lishiMain: Int) -> Bool {
    var confirmj: Int = 3
    var baseo: String! = String(cString: [97,116,114,97,99,97,108,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      confirmj %= Swift.max(confirmj % (Swift.max(baseo.count, 7)), 1)
   }
   for _ in 0 ..< 1 {
      confirmj |= 3 + baseo.count
   }
   while (confirmj > baseo.count) {
      baseo.append("\(3 * confirmj)")
      break
   }
   if 3 > (baseo.count << (Swift.min(1, labs(confirmj)))) {
      baseo.append("\(baseo.count | confirmj)")
   }
     let modityCollate: Int = 9102
     let preferredIndices: Float = 8472.0
    var  abcsAutoplaying:Bool = false
    abcsAutoplaying = modityCollate > 91
    abcsAutoplaying = preferredIndices > 70

    return abcsAutoplaying

}





    
    func webSocket(_ webSocket: SRWebSocket, didReceiveMessage message: Any) {

             unsafeIncludeDisappear(applyNick:3999.0, lishiMain:418)


       var parametersv: String! = String(cString: [111,99,99,117,112,105,101,100,0], encoding: .utf8)!
    var e_playerS: String! = String(cString: [109,117,108,116,105,101,110,100,0], encoding: .utf8)!
    var pasteboardR: Int = 3
    var containsE: String! = String(cString: [99,108,111,117,100,0], encoding: .utf8)!
      e_playerS.append("\(pasteboardR % (Swift.max(3, 7)))")

   for _ in 0 ..< 3 {
       var menuR: String! = String(cString: [114,101,115,105,100,101,110,99,101,0], encoding: .utf8)!
       var graphicsy: [String: Any]! = [String(cString: [99,111,108,117,109,110,0], encoding: .utf8)!:169, String(cString: [116,101,120,105,112,111,100,0], encoding: .utf8)!:384, String(cString: [98,105,97,115,101,100,0], encoding: .utf8)!:900]
       var find4: [String: Any]! = [String(cString: [119,114,105,116,97,98,108,101,0], encoding: .utf8)!:215, String(cString: [112,112,102,108,97,103,115,0], encoding: .utf8)!:219, String(cString: [100,101,115,116,114,111,121,105,110,103,0], encoding: .utf8)!:186]
       var callX: Float = 4.0
       var contentsP: Bool = false
       var restoreW: String! = String(cString: [115,117,99,99,101,115,115,99,98,0], encoding: .utf8)!
       var collate8: String! = String(cString: [97,116,117,114,97,116,105,111,110,0], encoding: .utf8)!
          var actionn: String! = String(cString: [109,112,111,115,0], encoding: .utf8)!
          var addx: Float = 0.0
         collate8.append("\(1)")
         actionn.append("\(Int(addx))")
         addx /= Swift.max(Float(3 ^ Int(addx)), 5)
      for _ in 0 ..< 3 {
         contentsP = collate8.hasSuffix("\(callX)")
      }
       var enginep: Float = 4.0
      if 1.51 > (enginep * 3.94) || 3.94 > (Float(graphicsy.keys.count) * enginep) {
         enginep *= Float(restoreW.count / 3)
      }
      while (2 == (5 | restoreW.count) || (restoreW.count | find4.values.count) == 5) {
         find4 = ["\(enginep)": ((contentsP ? 4 : 4) / (Swift.max(8, Int(enginep))))]
         break
      }
      repeat {
         restoreW = "\(find4.count)"
         if restoreW.count == 2412288 {
            break
         }
      } while (restoreW.count == 2412288) && (1 == (5 ^ restoreW.count))
      for _ in 0 ..< 3 {
         restoreW.append("\(menuR.count << (Swift.min(restoreW.count, 5)))")
      }
      while (2 == menuR.count) {
         callX *= Float(collate8.count)
         break
      }
      for _ in 0 ..< 2 {
          var resetN: Bool = false
          var sharedP: String! = String(cString: [102,119,97,108,115,104,0], encoding: .utf8)!
          var cellE: [Any]! = [String(cString: [115,116,114,102,116,105,109,101,0], encoding: .utf8)!, String(cString: [111,110,101,115,99,97,108,101,0], encoding: .utf8)!, String(cString: [115,99,97,110,116,97,98,108,101,0], encoding: .utf8)!]
          var seekl: String! = String(cString: [98,114,97,110,100,0], encoding: .utf8)!
         callX /= Swift.max(5, Float(restoreW.count))
         resetN = (String(cString:[103,0], encoding: .utf8)!) == sharedP && cellE.count == 36
         sharedP.append("\(((resetN ? 4 : 4)))")
         cellE.append(cellE.count)
         seekl.append("\(3)")
      }
      for _ in 0 ..< 2 {
         contentsP = 46.59 > enginep
      }
      for _ in 0 ..< 2 {
         find4 = ["\(contentsP)": ((contentsP ? 5 : 1) % (Swift.max(Int(callX), 5)))]
      }
         find4[menuR] = menuR.count & 1
       var receives: Double = 2.0
       var viiewF: Double = 3.0
         menuR = "\(graphicsy.values.count % 2)"
      parametersv.append("\(parametersv.count)")
   }
        if let text = message as? String {
            didReceiveMessageCallBlock?(text)
            print("WebSocket消息：\(text)")
        }
      parametersv = "\(1 + parametersv.count)"
    }

@discardableResult
 func phoneResponseAnimationOriginalColorLocale() -> Double {
    var displayD: [String: Any]! = [String(cString: [109,100,105,97,0], encoding: .utf8)!:String(cString: [98,116,114,101,101,0], encoding: .utf8)!, String(cString: [114,101,109,97,116,114,105,120,0], encoding: .utf8)!:String(cString: [110,105,115,116,95,49,95,57,53,0], encoding: .utf8)!]
    var avatarsA: [Any]! = [String(cString: [108,119,115,115,112,110,0], encoding: .utf8)!, String(cString: [114,101,110,111,114,109,0], encoding: .utf8)!, String(cString: [113,116,114,108,101,0], encoding: .utf8)!]
      avatarsA = [avatarsA.count]
      displayD = ["\(displayD.keys.count)": 2]
      avatarsA.append(displayD.keys.count)
     var ringSelect: Float = 4551.0
     let bufferRows: Int = 7032
    var getsockaddrCapture:Double = 0
    ringSelect += 91
    getsockaddrCapture /= Swift.max(Double(ringSelect), 1)
    getsockaddrCapture *= Double(bufferRows)

    return getsockaddrCapture

}





    
    func webSocket(_ webSocket: SRWebSocket, didFailWithError error: Error) {

             phoneResponseAnimationOriginalColorLocale()


       var attributesF: Double = 5.0
    var ismainA: Int = 3
      ismainA <<= Swift.min(labs(3), 5)
      ismainA += 2 % (Swift.max(ismainA, 10))

   repeat {
      ismainA >>= Swift.min(labs(2 % (Swift.max(ismainA, 6))), 2)
      if ismainA == 1795380 {
         break
      }
   } while (5 > ismainA) && (ismainA == 1795380)
   while ((attributesF / 5.10) >= 4.19 || 3.36 >= (attributesF / 5.10)) {
      ismainA &= ismainA / (Swift.max(Int(attributesF), 6))
      break
   }
        print("WebSocket 连接失败")
        connectFailedCallBlock?(error)
    }

    
    func webSocketDidOpen(_ webSocket: SRWebSocket) {
       var handingL: String! = String(cString: [114,101,115,97,109,112,108,101,107,104,122,0], encoding: .utf8)!
    var nick8: String! = String(cString: [112,101,114,109,117,116,97,116,105,111,110,115,0], encoding: .utf8)!
      nick8.append("\(2)")
       var hasA: String! = String(cString: [97,99,107,110,111,119,108,101,100,103,101,100,0], encoding: .utf8)!
      for _ in 0 ..< 1 {
          var randoms: String! = String(cString: [101,120,97,109,105,110,101,0], encoding: .utf8)!
         hasA = "\(2)"
         randoms.append("\(randoms.count << (Swift.min(labs(2), 2)))")
      }
      repeat {
         hasA = "\(hasA.count + 2)"
         if hasA == (String(cString:[95,120,100,52,105,55,55,116,101,0], encoding: .utf8)!) {
            break
         }
      } while (hasA == hasA) && (hasA == (String(cString:[95,120,100,52,105,55,55,116,101,0], encoding: .utf8)!))
       var linesh: String! = String(cString: [115,116,114,111,107,101,114,0], encoding: .utf8)!
      handingL.append("\(handingL.count)")

   while (handingL != nick8) {
      nick8.append("\((handingL == (String(cString:[107,0], encoding: .utf8)!) ? nick8.count : handingL.count))")
      break
   }
        print("WebSocket 连接成功")
        connectSuccessCallBlock?()
    }
}
