
import Foundation

import UIKit
import Speech
import AVFAudio
import AdSupport
import Accelerate
import SVProgressHUD

enum VClassO {
    case VMineCommon
    case VPopupRecords
}

class IDTShouJiao: NSObject {
private var ypricelabelShowWindow_1Str: String?
var paintResourcesDictionary: [String: Any]?
private var main_i: Float = 0.0


    
    let utils: NeoNuiTts = NeoNuiTts.get_instance()
    let voicePlayer: FFirst = FFirst()
    typealias CompletionHandler = (VClassO) -> Void
    var completionHandler: CompletionHandler?
    
    static let shared: IDTShouJiao = {
       var gifr: String! = String(cString: [112,101,114,115,105,115,116,97,110,116,0], encoding: .utf8)!
    var d_player_: String! = String(cString: [112,114,101,112,97,114,105,110,103,0], encoding: .utf8)!
      gifr = "\(2 / (Swift.max(8, d_player_.count)))"

        let instance = IDTShouJiao()
      d_player_.append("\(gifr.count)")
        instance.utils.delegate = instance
   for _ in 0 ..< 3 {
       var mineW: Double = 4.0
       var gressx: [String: Any]! = [String(cString: [116,101,114,109,105,110,97,116,101,100,0], encoding: .utf8)!:944, String(cString: [110,101,103,111,116,105,97,116,101,0], encoding: .utf8)!:630, String(cString: [100,99,116,115,117,98,0], encoding: .utf8)!:667]
       var socketW: [Any]! = [String(cString: [115,108,97,118,101,0], encoding: .utf8)!, String(cString: [114,119,103,116,0], encoding: .utf8)!, String(cString: [102,97,99,116,111,114,115,0], encoding: .utf8)!]
       var createX: Int = 3
      for _ in 0 ..< 3 {
         mineW *= Double(createX)
      }
      if !gressx.values.contains { $0 as? Int == socketW.count } {
          var aid6: String! = String(cString: [115,101,103,109,97,112,0], encoding: .utf8)!
          var selectP: Bool = true
          var alifastt: String! = String(cString: [111,110,103,111,105,110,103,0], encoding: .utf8)!
         gressx = [aid6: 3]
         aid6 = "\(alifastt.count << (Swift.min(5, alifastt.count)))"
         selectP = (String(cString:[50,0], encoding: .utf8)!) == alifastt
      }
          var codeH: Double = 2.0
         mineW += Double(createX)
         codeH /= Swift.max(Double(Int(codeH)), 5)
         gressx = ["\(createX)": 3 + Int(mineW)]
      while ((socketW.count >> (Swift.min(labs(1), 5))) < 5 || (1 >> (Swift.min(2, gressx.count))) < 5) {
         socketW.append(2 & socketW.count)
         break
      }
         createX &= 2 & gressx.values.count
      repeat {
         createX %= Swift.max(Int(mineW), 2)
         if 4285612 == createX {
            break
         }
      } while (4285612 == createX) && (1 < (createX | gressx.count))
         gressx["\(mineW)"] = 3 << (Swift.min(labs(Int(mineW)), 5))
      for _ in 0 ..< 3 {
         gressx["\(createX)"] = socketW.count ^ 2
      }
      repeat {
          var timelabelE: String! = String(cString: [108,115,112,105,0], encoding: .utf8)!
          var int_og: Float = 3.0
          var gestureS: Double = 3.0
          var browserT: Double = 3.0
         createX %= Swift.max(createX << (Swift.min(5, labs(3))), 3)
         timelabelE = "\(timelabelE.count)"
         int_og *= Float(Int(int_og) << (Swift.min(5, labs(Int(browserT)))))
         gestureS += Double(3)
         browserT /= Swift.max(1, Double(2))
         if createX == 2735178 {
            break
         }
      } while ((createX / (Swift.max(5, 10))) <= 5 || (createX / (Swift.max(5, 4))) <= 2) && (createX == 2735178)
         mineW += Double(createX)
      while (gressx["\(mineW)"] != nil) {
         gressx["\(createX)"] = socketW.count % (Swift.max(2, 3))
         break
      }
      d_player_.append("\((d_player_ == (String(cString:[49,0], encoding: .utf8)!) ? d_player_.count : socketW.count))")
   }
        instance.voicePlayer.delegate = instance
   repeat {
       var displayE: [Any]! = [164, 265, 275]
       var imagesr: Float = 2.0
       var enabledX: String! = String(cString: [99,104,111,105,99,101,115,0], encoding: .utf8)!
         displayE = [3 | Int(imagesr)]
         imagesr += (Float(enabledX == (String(cString:[51,0], encoding: .utf8)!) ? Int(imagesr) : enabledX.count))
          var parameterf: String! = String(cString: [116,109,109,98,0], encoding: .utf8)!
          var int_qk: [String: Any]! = [String(cString: [99,97,108,99,117,108,97,116,101,0], encoding: .utf8)!:String(cString: [110,101,97,114,0], encoding: .utf8)!, String(cString: [115,117,98,116,114,97,99,116,0], encoding: .utf8)!:String(cString: [105,112,109,111,118,105,101,0], encoding: .utf8)!, String(cString: [115,116,114,110,99,97,115,101,99,109,112,0], encoding: .utf8)!:String(cString: [97,108,111,99,0], encoding: .utf8)!]
          var channelx: Double = 3.0
         enabledX.append("\(parameterf.count)")
         parameterf = "\(Int(channelx) - int_qk.keys.count)"
         int_qk["\(channelx)"] = Int(channelx) + int_qk.values.count
         displayE = [displayE.count + Int(imagesr)]
          var avatar4: String! = String(cString: [115,98,103,112,0], encoding: .utf8)!
          var promptP: Bool = false
         displayE = [enabledX.count]
         avatar4 = "\(avatar4.count)"
         promptP = (avatar4.count | avatar4.count) < 58
         imagesr += Float(displayE.count)
      if (enabledX.count % (Swift.max(8, displayE.count))) == 3 {
         displayE = [enabledX.count]
      }
      for _ in 0 ..< 2 {
          var generator6: Float = 1.0
          var config2: String! = String(cString: [101,97,99,104,0], encoding: .utf8)!
          var servicej: String! = String(cString: [112,117,116,110,117,109,112,97,115,115,101,115,0], encoding: .utf8)!
          var line4: Double = 5.0
          var downt: String! = String(cString: [105,110,105,116,105,97,116,111,114,0], encoding: .utf8)!
         enabledX.append("\(displayE.count / 1)")
         generator6 /= Swift.max(Float(downt.count % 3), 5)
         config2.append("\((config2 == (String(cString:[72,0], encoding: .utf8)!) ? config2.count : Int(generator6)))")
         servicej = "\(3 + Int(generator6))"
         line4 -= Double(1)
         downt.append("\((servicej == (String(cString:[87,0], encoding: .utf8)!) ? config2.count : servicej.count))")
      }
      while (!enabledX.hasPrefix("\(imagesr)")) {
          var stylesV: Double = 1.0
          var speaks: [Any]! = [7369]
         imagesr -= (Float(enabledX == (String(cString:[85,0], encoding: .utf8)!) ? Int(stylesV) : enabledX.count))
         stylesV *= Double(speaks.count << (Swift.min(labs(3), 3)))
         speaks = [speaks.count]
         break
      }
      d_player_.append("\(d_player_.count << (Swift.min(5, gifr.count)))")
      if d_player_.count == 3445410 {
         break
      }
   } while (d_player_.count == 3445410) && (d_player_ == String(cString:[56,0], encoding: .utf8)! && gifr == String(cString:[57,0], encoding: .utf8)!)
        return instance
    }()

@discardableResult
 func detectMainPictureContentCenter(createAudio: Double) -> Double {
    var sumG: Double = 5.0
    var evetr: Int = 0
   while (4.38 == (Double(evetr) * sumG) && (1 / (Swift.max(6, evetr))) == 2) {
      evetr -= evetr
      break
   }
      evetr |= evetr << (Swift.min(3, labs(1)))
   while (3.97 > sumG) {
      evetr /= Swift.max(5, Int(sumG) % (Swift.max(5, evetr)))
      break
   }
       var desclabelG: [String: Any]! = [String(cString: [116,104,101,111,114,97,0], encoding: .utf8)!:835, String(cString: [105,110,116,114,97,120,104,117,102,0], encoding: .utf8)!:955]
       var pnewsk: String! = String(cString: [97,108,98,117,109,115,0], encoding: .utf8)!
       var thresholdC: String! = String(cString: [115,121,110,99,119,111,114,100,115,0], encoding: .utf8)!
         desclabelG = [pnewsk: 3 << (Swift.min(3, pnewsk.count))]
      for _ in 0 ..< 2 {
          var zhidingesx: Float = 0.0
         pnewsk = "\(1)"
         zhidingesx += Float(Int(zhidingesx) + 1)
      }
      if pnewsk.count <= thresholdC.count {
         thresholdC = "\(1)"
      }
         pnewsk.append("\(pnewsk.count & 3)")
       var stylesW: [Any]! = [181, 907, 764]
       var keyz: [Any]! = [856, 886]
         stylesW.append(((String(cString:[87,0], encoding: .utf8)!) == pnewsk ? pnewsk.count : keyz.count))
      for _ in 0 ..< 3 {
         thresholdC.append("\(2)")
      }
      for _ in 0 ..< 2 {
         keyz = [keyz.count | 3]
      }
          var menuQ: String! = String(cString: [112,114,111,98,0], encoding: .utf8)!
          var instanceE: [String: Any]! = [String(cString: [115,119,105,122,122,108,101,100,0], encoding: .utf8)!:UILabel()]
         stylesW = [2]
         menuQ.append("\(instanceE.keys.count)")
         instanceE = ["\(instanceE.values.count)": ((String(cString:[104,0], encoding: .utf8)!) == menuQ ? menuQ.count : instanceE.count)]
      evetr <<= Swift.min(1, labs(evetr))
   return sumG

}





    
    func startPlay(fontName: String = "", message: String, completionHandler: CompletionHandler?) {

             detectMainPictureContentCenter(createAudio:6696.0)


       var scaleL: String! = String(cString: [100,105,115,116,112,111,105,110,116,0], encoding: .utf8)!
    var configE: Int = 4
      scaleL.append("\(configE / 3)")

       var ossh: String! = String(cString: [115,116,105,99,107,101,114,115,101,116,0], encoding: .utf8)!
       var nameD: Double = 2.0
       var editY: String! = String(cString: [115,112,101,97,107,0], encoding: .utf8)!
      while (5 <= (editY.count + Int(nameD)) && 4.31 <= (3.63 + nameD)) {
         nameD -= Double(ossh.count | 3)
         break
      }
      for _ in 0 ..< 1 {
          var base_: String! = String(cString: [112,114,117,110,101,0], encoding: .utf8)!
          var remarkn: String! = String(cString: [115,116,111,114,97,103,101,0], encoding: .utf8)!
          var descj: [String: Any]! = [String(cString: [111,98,106,0], encoding: .utf8)!:48, String(cString: [102,112,117,116,115,0], encoding: .utf8)!:9]
         ossh.append("\(editY.count & 3)")
         base_.append("\(descj.count ^ 3)")
         remarkn.append("\(descj.count)")
      }
          var numberi: String! = String(cString: [103,101,116,97,117,120,118,97,108,0], encoding: .utf8)!
          var action7: Float = 1.0
          var numberlabelm: [Any]! = [302, 455, 239]
         ossh.append("\(1 + Int(action7))")
         numberi.append("\(((String(cString:[79,0], encoding: .utf8)!) == numberi ? numberlabelm.count : numberi.count))")
         action7 *= Float(numberlabelm.count >> (Swift.min(labs(3), 2)))
         nameD /= Swift.max(4, Double(1 << (Swift.min(1, labs(Int(nameD))))))
         editY = "\((ossh == (String(cString:[103,0], encoding: .utf8)!) ? Int(nameD) : ossh.count))"
      if 3 > editY.count {
          var primeZ: [String: Any]! = [String(cString: [109,117,116,101,120,101,115,0], encoding: .utf8)!:443, String(cString: [102,105,110,97,108,105,115,101,114,0], encoding: .utf8)!:931]
         ossh.append("\((editY == (String(cString:[76,0], encoding: .utf8)!) ? editY.count : primeZ.count))")
      }
      if editY.hasPrefix("\(nameD)") {
         nameD /= Swift.max(Double(1), 3)
      }
      repeat {
         nameD /= Swift.max(Double(editY.count / 1), 5)
         if 4496276.0 == nameD {
            break
         }
      } while (4496276.0 == nameD) && (ossh.contains("\(nameD)"))
      while (nameD == 4.52) {
         nameD *= Double(editY.count + 2)
         break
      }
      scaleL.append("\(scaleL.count + 1)")
        
        stopPlay(false)
        
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.3) { [self]  in
   while (1 < scaleL.count) {
      configE <<= Swift.min(labs(1), 5)
      break
   }
            
            self.completionHandler = completionHandler
      scaleL = "\(scaleL.count + configE)"
            
            utils.nui_tts_initialize(getInitParam(), logLevel: NuiSdkLogLevel(0), saveLog: true)

            if fontName == "" {
                if let font_name: String = UserDefaults.standard.object(forKey: "font_name") as? String {
                    utils.nui_tts_set_param("font_name", value: font_name)
                }else {
                    utils.nui_tts_set_param("font_name", value: "zhimiao_emo")
                }
            }else {
                utils.nui_tts_set_param("font_name", value: fontName)
            }
            let m_imageImage = UserDefaults.standard.float(forKey: "rate")
            if m_imageImage > 0 {
                utils.nui_tts_set_param("speed_level", value: String(format: "%0.2f", m_imageImage))
            }
            utils.nui_tts_play("1", taskId: "", text: message)
            
        }
    }

    
    func stopPlay(_ isBlock: Bool = true) {
       var listz: String! = String(cString: [98,108,111,99,107,100,99,0], encoding: .utf8)!
    var basek: Bool = true
    var elevtW: Bool = false
    var flowo: Bool = false
   for _ in 0 ..< 3 {
      listz = "\(((elevtW ? 1 : 3) * (basek ? 3 : 5)))"
   }

      listz.append("\(((basek ? 3 : 4)))")
        
        voicePlayer.stop()
   if !elevtW {
      basek = !elevtW
   }
        utils.nui_tts_cancel(nil)
   if !basek {
       var snew_4c: Int = 0
       var displayN: Double = 3.0
       var shouc: Double = 2.0
      for _ in 0 ..< 2 {
         displayN -= Double(snew_4c / (Swift.max(10, Int(displayN))))
      }
         shouc += Double(snew_4c + Int(shouc))
      while (4.58 <= (Double(snew_4c + Int(shouc)))) {
          var beginh: String! = String(cString: [98,97,114,114,101,116,116,0], encoding: .utf8)!
         snew_4c ^= (beginh == (String(cString:[106,0], encoding: .utf8)!) ? beginh.count : Int(shouc))
         break
      }
      repeat {
         displayN /= Swift.max(Double(snew_4c & Int(displayN)), 5)
         if 2463322.0 == displayN {
            break
         }
      } while (2463322.0 == displayN) && ((displayN / 5.82) < 5.29)
      for _ in 0 ..< 3 {
         displayN += Double(3 + Int(displayN))
      }
      basek = 10.82 < displayN
   }
        if isBlock { self.completionHandler?(.VPopupRecords) }
        
    }
}

func createPath() -> String {
       var array7: Double = 0.0
    var contentsC: Int = 5
       var selectQ: String! = String(cString: [118,99,114,101,97,116,101,0], encoding: .utf8)!
       var speedsC: Double = 4.0
       var contains1: Float = 5.0
       var applicationF: String! = String(cString: [114,111,98,117,115,116,0], encoding: .utf8)!
       var commonP: String! = String(cString: [100,110,120,104,100,101,110,99,0], encoding: .utf8)!
       var engineN: Float = 1.0
       var tableO: Float = 4.0
      while ((5 & commonP.count) <= 3) {
         engineN -= Float(Int(contains1))
         break
      }
      while (5.73 <= (contains1 / 3.29)) {
         contains1 *= Float(Int(tableO) << (Swift.min(labs(3), 3)))
         break
      }
         commonP.append("\(1)")
      while (applicationF == commonP) {
         commonP = "\(applicationF.count + 2)"
         break
      }
      while ((Double(contains1) - speedsC) >= 2.80) {
         contains1 += Float(2)
         break
      }
         tableO += Float(Int(speedsC))
         applicationF = "\(Int(engineN) + 3)"
      array7 *= Double(Int(speedsC) ^ Int(contains1))
      selectQ.append("\(3 << (Swift.min(5, selectQ.count)))")

   while ((contentsC * 5) >= 4) {
      contentsC += Int(array7) + contentsC
      break
   }
       
    let mine = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true)
   for _ in 0 ..< 3 {
       var press4: [Any]! = [UILabel()]
       var reusableK: String! = String(cString: [99,117,100,97,115,99,97,108,101,0], encoding: .utf8)!
       var responseX: String! = String(cString: [99,111,108,111,114,102,117,108,0], encoding: .utf8)!
       var decibel0: Double = 0.0
       var ismainh: [Any]! = [870, 294]
         decibel0 /= Swift.max(3, Double(reusableK.count * 1))
      for _ in 0 ..< 1 {
          var avatara: String! = String(cString: [105,110,115,116,0], encoding: .utf8)!
         press4 = [responseX.count << (Swift.min(1, labs(Int(decibel0))))]
         avatara.append("\(1)")
      }
       var unselectedu: Double = 1.0
      if ismainh.count <= 2 {
         unselectedu /= Swift.max(3, Double(reusableK.count))
      }
      if (unselectedu / (Swift.max(6, decibel0))) > 4.56 || (unselectedu * 4.56) > 5.72 {
          var didS: Bool = false
         unselectedu *= Double(Int(decibel0))
         didS = (!didS ? didS : didS)
      }
      if Double(ismainh.count) <= decibel0 {
         ismainh.append(((String(cString:[56,0], encoding: .utf8)!) == responseX ? responseX.count : Int(unselectedu)))
      }
       var resourcesO: String! = String(cString: [115,108,111,119,101,115,116,0], encoding: .utf8)!
      while ((decibel0 + 3.10) >= 4.28) {
         decibel0 *= Double(2 ^ ismainh.count)
         break
      }
       var parameter4: Bool = true
      if (Double(ismainh.count) - unselectedu) < 4.63 {
         ismainh.append(1)
      }
          var lnewsc: [Any]! = [915, 521]
          var loginZ: Bool = false
         decibel0 += Double(press4.count - 1)
         lnewsc.append(3)
         loginZ = lnewsc.count < lnewsc.count
       var modityv: Bool = false
      repeat {
         unselectedu /= Swift.max(Double(press4.count), 4)
         if 2631055.0 == unselectedu {
            break
         }
      } while (unselectedu >= Double(resourcesO.count)) && (2631055.0 == unselectedu)
      if 3 == ismainh.count {
         ismainh.append(responseX.count >> (Swift.min(labs(1), 5)))
      }
      repeat {
         ismainh = [resourcesO.count * responseX.count]
         if 4053962 == ismainh.count {
            break
         }
      } while (2.42 <= unselectedu) && (4053962 == ismainh.count)
      array7 /= Swift.max(Double(1), 4)
   }
    let nick = mine[0]
   
    let accountlabel = FileManager.default
    let numberCel = (nick as NSString).appendingPathComponent("voices")
    
    do {
        try accountlabel.createDirectory(atPath: numberCel, withIntermediateDirectories: true, attributes: nil)
        print("文件夹创建成功")
    } catch {
        print("文件夹创建失败")
    }
    return numberCel
}

func getInitParam() -> String {
       var remarkt: String! = String(cString: [105,102,105,108,116,101,114,0], encoding: .utf8)!
    var meali: Bool = true
       var gundongW: String! = String(cString: [119,121,99,104,101,112,114,111,111,102,0], encoding: .utf8)!
       var json8: String! = String(cString: [97,118,114,101,115,97,109,112,108,101,114,101,115,0], encoding: .utf8)!
      if gundongW == json8 {
          var finisha: String! = String(cString: [115,117,109,115,113,0], encoding: .utf8)!
          var receiver: String! = String(cString: [114,109,117,108,116,105,112,108,105,99,97,116,105,111,110,0], encoding: .utf8)!
         json8 = "\(json8.count << (Swift.min(2, gundongW.count)))"
         finisha.append("\(finisha.count >> (Swift.min(4, receiver.count)))")
         receiver = "\(2)"
      }
      while (!gundongW.hasPrefix(json8)) {
         gundongW = "\(gundongW.count)"
         break
      }
         gundongW.append("\(gundongW.count / (Swift.max(1, 3)))")
      repeat {
          var evetI: [Any]! = [265, 566]
          var cancelI: String! = String(cString: [99,108,108,105,0], encoding: .utf8)!
          var delegate_rqD: String! = String(cString: [104,117,101,115,0], encoding: .utf8)!
          var handlery: Bool = false
          var speeds7: Double = 4.0
         gundongW = "\(cancelI.count - Int(speeds7))"
         evetI = [((handlery ? 5 : 4) * delegate_rqD.count)]
         cancelI = "\(delegate_rqD.count)"
         speeds7 += Double(2)
         if gundongW == (String(cString:[95,120,95,0], encoding: .utf8)!) {
            break
         }
      } while (!gundongW.contains(json8)) && (gundongW == (String(cString:[95,120,95,0], encoding: .utf8)!))
         json8 = "\(json8.count * 2)"
       var relationE: String! = String(cString: [116,111,108,108,0], encoding: .utf8)!
       var weak_la: String! = String(cString: [114,100,102,116,0], encoding: .utf8)!
      remarkt = "\(((meali ? 2 : 1) << (Swift.min(labs(3), 5))))"

       
    guard let strResourcesBundle = Bundle.main.path(forResource: "Resources", ofType: "bundle") else {
        return ""
    }
    
    let empty = Bundle(path: strResourcesBundle)?.resourcePath ?? ""
   for _ in 0 ..< 1 {
       var prompth: Double = 1.0
       var navigationo: [String: Any]! = [String(cString: [116,101,115,116,105,109,103,97,114,105,0], encoding: .utf8)!:true]
      if 4.47 == (Double(navigationo.keys.count) - prompth) {
         navigationo = ["\(navigationo.count)": 2]
      }
      for _ in 0 ..< 1 {
          var type_wz: Int = 2
         navigationo = ["\(navigationo.count)": Int(prompth) & 3]
         type_wz >>= Swift.min(4, labs(type_wz))
      }
      if navigationo["\(prompth)"] == nil {
         prompth *= Double(1)
      }
          var popup9: Float = 2.0
         navigationo["\(prompth)"] = 3
         popup9 -= Float(2 & Int(popup9))
      while ((prompth / 2.18) > 4.10 && (prompth / (Swift.max(Double(navigationo.keys.count), 4))) > 2.18) {
         prompth -= Double(navigationo.count)
         break
      }
       var error7: Double = 1.0
       var header3: Double = 3.0
      remarkt = "\(navigationo.values.count | 3)"
   }
    let userdata = ASIdentifierManager.shared().advertisingIdentifier.uuidString
   if 2 > remarkt.count || meali {
       var commonN: String! = String(cString: [115,117,98,115,99,114,105,98,97,98,108,101,115,0], encoding: .utf8)!
       var playI: Double = 0.0
       var successW: Int = 0
       var avatar4: [Any]! = [3501]
       var flowu: String! = String(cString: [108,111,116,116,105,101,112,97,114,115,101,114,0], encoding: .utf8)!
         avatar4.append(successW % 1)
      for _ in 0 ..< 2 {
         commonN = "\(1 | Int(playI))"
      }
      repeat {
          var qlabelB: [Any]! = [206, 836, 728]
         playI -= Double(1 % (Swift.max(3, Int(playI))))
         qlabelB = [1 + qlabelB.count]
         if 3846036.0 == playI {
            break
         }
      } while (3846036.0 == playI) && (flowu.count >= Int(playI))
      while ((successW * avatar4.count) <= 2 || 4 <= (successW * 2)) {
         successW -= avatar4.count >> (Swift.min(5, labs(Int(playI))))
         break
      }
      for _ in 0 ..< 2 {
         avatar4.append(avatar4.count)
      }
      repeat {
         playI *= (Double((String(cString:[71,0], encoding: .utf8)!) == commonN ? commonN.count : avatar4.count))
         if 857830.0 == playI {
            break
         }
      } while (successW <= Int(playI)) && (857830.0 == playI)
      repeat {
         playI += (Double(flowu == (String(cString:[48,0], encoding: .utf8)!) ? avatar4.count : flowu.count))
         if playI == 4108705.0 {
            break
         }
      } while (playI == 4108705.0) && (3.98 == (playI + 1.19))
      repeat {
          var commonn: Int = 0
          var firstq: [String: Any]! = [String(cString: [102,97,117,99,101,116,115,0], encoding: .utf8)!:552, String(cString: [105,108,98,99,0], encoding: .utf8)!:751]
          var prefix__q: Float = 2.0
          var freelabelu: Float = 1.0
          var audiof: String! = String(cString: [115,112,101,99,0], encoding: .utf8)!
         commonN.append("\(firstq.values.count)")
         commonn %= Swift.max(commonn ^ 1, 5)
         firstq = ["\(freelabelu)": commonn]
         prefix__q *= (Float(audiof == (String(cString:[110,0], encoding: .utf8)!) ? Int(prefix__q) : audiof.count))
         freelabelu /= Swift.max(Float(commonn), 3)
         if commonN.count == 602208 {
            break
         }
      } while (Int(playI) == commonN.count) && (commonN.count == 602208)
      while (4.47 > playI) {
         playI /= Swift.max((Double((String(cString:[85,0], encoding: .utf8)!) == commonN ? Int(playI) : commonN.count)), 4)
         break
      }
          var refresh6: Double = 0.0
          var prefix_2gp: Bool = false
          var error9: Float = 4.0
         playI /= Swift.max(Double(avatar4.count), 2)
         refresh6 += Double(Int(error9) + 1)
         prefix_2gp = error9 > 31.58
         flowu.append("\(successW)")
      for _ in 0 ..< 1 {
          var responderc: String! = String(cString: [116,114,97,110,115,99,101,105,118,101,114,0], encoding: .utf8)!
          var playe: String! = String(cString: [99,111,110,116,0], encoding: .utf8)!
          var queueS: Float = 5.0
          var typelabell: Int = 5
         flowu.append("\(Int(playI))")
         responderc = "\(typelabell)"
         playe.append("\(((String(cString:[76,0], encoding: .utf8)!) == responderc ? typelabell : responderc.count))")
         queueS += Float(playe.count)
      }
       var numberlabelh: Double = 0.0
       var screenk: Double = 4.0
         avatar4 = [successW & 3]
      meali = 94 <= flowu.count
   }
 
    let register_f = createPath()
   repeat {
      meali = meali && remarkt.count < 55
      if meali ? !meali : meali {
         break
      }
   } while (meali ? !meali : meali) && (remarkt.hasPrefix("\(meali)"))
    
    if let token = UserDefaults.standard.string(forKey: "AliToken") {
        
        var unselected = [String: Any]()
        unselected["app_key"] = "FwsOLV8DKcHopkcl"
        unselected["token"] = token
        unselected["workspace"] = empty
        unselected["debug_path"] = register_f
        unselected["device_id"] = userdata
        unselected["url"] = "wss://nls-gateway.cn-shanghai.aliyuncs.com:443/ws/v1"
        unselected["mode_type"] = "2"

        do {
            let num = try JSONSerialization.data(withJSONObject: unselected, options: .prettyPrinted)
            if let jsonStr = String(data: num, encoding: .utf8) {
                return jsonStr
            }
        } catch {
            print("JSONSerialization error: \(error)")
        }
    }else {
        SVProgressHUD.showError(withStatus: "阿里Token无效")
    }

    return ""
}

func checkAliToken() {
       var loginK: Bool = true
    var taskP: Double = 0.0
   repeat {
      loginK = 90.62 > taskP
      if loginK ? !loginK : loginK {
         break
      }
   } while (loginK ? !loginK : loginK) && (!loginK)

    
    VKXBase.shared.post(urlSuffix: "/app/getAliyunToken") { (result: Result<HACItemsGraphics, UAlifastPhoney>) in
   while (loginK || (taskP - 5.84) == 3.39) {
      taskP /= Swift.max(4, Double(Int(taskP) - 3))
      break
   }
        switch result {
        case .success(let model):
            if model.code == 200 {
                UserDefaults.standard.setValue(model.data, forKey: "AliToken")
   for _ in 0 ..< 2 {
      taskP += (Double((loginK ? 1 : 1) / (Swift.max(Int(taskP), 9))))
   }
                print("阿里Token\(model.data)")
   if taskP >= 2.17 {
      taskP *= Double(1 % (Swift.max(Int(taskP), 10)))
   }
                UserDefaults.standard.synchronize()
            }
            
            break
        case.failure(_):
            SVProgressHUD.showError(withStatus: "获取Token失败");
            break
        }
    }

}


extension IDTShouJiao: NeoNuiTtsDelegate, DIDCRegister {

@discardableResult
 func referenceQuestionPlayer() -> [Any]! {
    var gundongo: Float = 3.0
    var free5: String! = String(cString: [115,116,97,116,0], encoding: .utf8)!
      free5.append("\(2)")
      gundongo += (Float((String(cString:[56,0], encoding: .utf8)!) == free5 ? free5.count : Int(gundongo)))
      gundongo -= Float(free5.count)
   repeat {
      free5.append("\(Int(gundongo))")
      if 3647615 == free5.count {
         break
      }
   } while (4.98 > gundongo) && (3647615 == free5.count)
     let completionFlow: Float = 8444.0
    var  intreadwriteAge = [Any]()
    intreadwriteAge.append(completionFlow)

    return intreadwriteAge

}





    
    func onNuiTtsUserdataCallback(_ info: UnsafeMutablePointer<CChar>!, infoLen info_len: Int32, buffer: UnsafeMutablePointer<CChar>!, len: Int32, taskId task_id: UnsafeMutablePointer<CChar>!) {

             referenceQuestionPlayer()


       var rowg: Double = 3.0
    var commentu: Double = 5.0
   for _ in 0 ..< 3 {
      commentu *= Double(Int(commentu) & Int(rowg))
   }

   while ((rowg / (Swift.max(commentu, 6))) > 5.18 || 1.20 > (5.18 / (Swift.max(10, rowg)))) {
       var bodyT: String! = String(cString: [99,111,112,121,105,110,103,0], encoding: .utf8)!
          var elevtE: Bool = false
          var recognitiong: Double = 0.0
         bodyT = "\(bodyT.count % (Swift.max(3, 10)))"
         elevtE = 79.34 >= recognitiong || !elevtE
         recognitiong += Double(Int(recognitiong) / 1)
      for _ in 0 ..< 2 {
         bodyT.append("\(bodyT.count % (Swift.max(bodyT.count, 7)))")
      }
      repeat {
         bodyT = "\(bodyT.count)"
         if bodyT == (String(cString:[111,112,97,53,0], encoding: .utf8)!) {
            break
         }
      } while (bodyT == (String(cString:[111,112,97,53,0], encoding: .utf8)!)) && (!bodyT.hasSuffix("\(bodyT.count)"))
      commentu /= Swift.max(5, Double(Int(rowg)))
      break
   }

   repeat {
       var item3: String! = String(cString: [114,101,115,111,117,114,99,101,0], encoding: .utf8)!
       var num9: String! = String(cString: [115,97,110,100,98,111,120,0], encoding: .utf8)!
       var remarki: Double = 2.0
       var delegate_gT: [Any]! = [996, 11, 190]
       var btn5: [Any]! = [505, 277]
       var stylesO: [Any]! = [693, 152, 146]
      while (1 < (4 ^ btn5.count)) {
         btn5 = [item3.count]
         break
      }
         stylesO.append(num9.count)
      if Double(stylesO.count) == remarki {
         remarki /= Swift.max(4, Double(2 * stylesO.count))
      }
         remarki *= Double(delegate_gT.count)
      if 5 == (btn5.count % (Swift.max(num9.count, 4))) || (num9.count % (Swift.max(5, 7))) == 1 {
          var g_view4: [String: Any]! = [String(cString: [109,97,107,101,109,97,99,112,107,103,0], encoding: .utf8)!:781, String(cString: [117,110,105,118,101,114,115,97,108,0], encoding: .utf8)!:907]
          var launchx: String! = String(cString: [99,108,101,97,114,115,0], encoding: .utf8)!
          var responseo: String! = String(cString: [99,108,97,115,115,110,97,109,101,0], encoding: .utf8)!
          var fixedo: Double = 1.0
         btn5 = [num9.count & 2]
         g_view4[launchx] = 1 | responseo.count
         launchx.append("\(g_view4.keys.count / (Swift.max(responseo.count, 5)))")
         fixedo -= (Double(launchx == (String(cString:[115,0], encoding: .utf8)!) ? launchx.count : responseo.count))
      }
          var browserk: Bool = false
         delegate_gT.append(stylesO.count & 3)
      if 5 == (btn5.count % (Swift.max(2, delegate_gT.count))) {
          var lengthY: [String: Any]! = [String(cString: [99,111,110,116,97,105,110,97,98,108,101,0], encoding: .utf8)!:4735.0]
          var messagei: String! = String(cString: [98,97,99,107,115,112,97,99,101,0], encoding: .utf8)!
         delegate_gT.append(3 / (Swift.max(6, delegate_gT.count)))
         lengthY["\(messagei)"] = (messagei == (String(cString:[57,0], encoding: .utf8)!) ? messagei.count : lengthY.keys.count)
      }
          var holderlabelw: [Any]! = [936, 95, 635]
         num9 = "\(holderlabelw.count + 3)"
      for _ in 0 ..< 1 {
          var hnew_j1R: Double = 2.0
          var modelq: String! = String(cString: [101,110,115,117,114,101,100,0], encoding: .utf8)!
          var attributesC: String! = String(cString: [101,105,112,118,0], encoding: .utf8)!
          var class_qd: Float = 4.0
          var indicess: String! = String(cString: [115,117,98,115,101,103,109,101,110,116,0], encoding: .utf8)!
         stylesO.append(stylesO.count % (Swift.max(item3.count, 7)))
         hnew_j1R /= Swift.max(4, Double(Int(hnew_j1R) << (Swift.min(3, labs(Int(class_qd))))))
         modelq.append("\(((String(cString:[65,0], encoding: .utf8)!) == indicess ? indicess.count : Int(class_qd)))")
         attributesC = "\(1 << (Swift.min(1, attributesC.count)))"
      }
         stylesO.append(delegate_gT.count)
      while ((stylesO.count | item3.count) < 3 && (item3.count | stylesO.count) < 3) {
         item3 = "\(1 >> (Swift.min(5, delegate_gT.count)))"
         break
      }
      rowg /= Swift.max(3, Double(item3.count & 1))
      if 3331052.0 == rowg {
         break
      }
   } while ((commentu / 3.50) <= 3.63 && (commentu / 3.50) <= 3.79) && (3331052.0 == rowg)
        if len > 0 {
            voicePlayer.write(buffer, length: Int32(len))
        }
       
   if (rowg * 5.1) < 5.96 || 4.34 < (commentu - 5.1) {
       var userO: String! = String(cString: [100,117,112,108,101,120,0], encoding: .utf8)!
       var headery: String! = String(cString: [115,97,100,100,0], encoding: .utf8)!
       var closeS: [String: Any]! = [String(cString: [115,112,108,105,116,0], encoding: .utf8)!:522, String(cString: [116,109,109,98,114,0], encoding: .utf8)!:355]
       var speedk: Bool = true
      repeat {
         closeS = [userO: 1 / (Swift.max(2, headery.count))]
         if closeS.count == 1795664 {
            break
         }
      } while (closeS.keys.count <= headery.count) && (closeS.count == 1795664)
      if 4 > headery.count {
         headery = "\(((speedk ? 1 : 1) | 3))"
      }
      repeat {
         closeS[userO] = 1
         if closeS.count == 1920889 {
            break
         }
      } while (closeS.count == 1920889) && ((1 << (Swift.min(3, closeS.keys.count))) >= 1)
      for _ in 0 ..< 1 {
         headery = "\((headery == (String(cString:[110,0], encoding: .utf8)!) ? headery.count : (speedk ? 4 : 5)))"
      }
       var lastN: String! = String(cString: [97,98,117,102,102,101,114,115,105,110,107,0], encoding: .utf8)!
      repeat {
         closeS = [headery: headery.count - lastN.count]
         if 4525116 == closeS.count {
            break
         }
      } while (4525116 == closeS.count) && (4 >= (lastN.count % 3))
       var audioB: String! = String(cString: [112,111,115,116,98,111,120,0], encoding: .utf8)!
       var playingl: String! = String(cString: [114,110,103,115,0], encoding: .utf8)!
         playingl = "\(((speedk ? 2 : 2)))"
      repeat {
         speedk = userO.count >= 55
         if speedk ? !speedk : speedk {
            break
         }
      } while (speedk) && (speedk ? !speedk : speedk)
      repeat {
         userO = "\(lastN.count % 1)"
         if userO == (String(cString:[119,112,54,0], encoding: .utf8)!) {
            break
         }
      } while (3 <= userO.count) && (userO == (String(cString:[119,112,54,0], encoding: .utf8)!))
         userO.append("\(2)")
      if closeS.values.count >= 2 && 1 >= (closeS.values.count ^ 2) {
          var taskg: String! = String(cString: [97,120,105,115,0], encoding: .utf8)!
          var networko: [String: Any]! = [String(cString: [105,110,116,101,114,118,97,108,115,0], encoding: .utf8)!:457, String(cString: [107,102,109,111,100,101,115,0], encoding: .utf8)!:782]
         speedk = networko.count >= 63 || !speedk
         taskg = "\(taskg.count - taskg.count)"
         networko = [taskg: (taskg == (String(cString:[115,0], encoding: .utf8)!) ? taskg.count : taskg.count)]
      }
      rowg += Double(headery.count ^ 1)
   }
    }

@discardableResult
 func recognitionObjectProEnter() -> String! {
    var avatars: [Any]! = [String(cString: [97,108,112,105,110,101,0], encoding: .utf8)!, String(cString: [108,101,118,101,108,115,0], encoding: .utf8)!, String(cString: [116,111,111,108,99,104,97,105,110,0], encoding: .utf8)!]
    var userdata5: Float = 4.0
   for _ in 0 ..< 2 {
      avatars.append(Int(userdata5))
   }
   if !avatars.contains { $0 as? Float == userdata5 } {
       var currentZ: String! = String(cString: [98,105,116,105,122,101,110,0], encoding: .utf8)!
       var refreshl: String! = String(cString: [101,97,115,121,0], encoding: .utf8)!
       var handlerk: Double = 0.0
         refreshl.append("\(1)")
       var dlabel7: String! = String(cString: [100,105,102,102,101,114,101,110,99,101,115,0], encoding: .utf8)!
      for _ in 0 ..< 2 {
         dlabel7.append("\(currentZ.count)")
      }
      for _ in 0 ..< 3 {
         dlabel7 = "\(dlabel7.count)"
      }
      if 3 >= currentZ.count {
         refreshl.append("\(refreshl.count - 2)")
      }
      if dlabel7 == currentZ {
         currentZ = "\(2)"
      }
      repeat {
         refreshl.append("\(2)")
         if 2258426 == refreshl.count {
            break
         }
      } while (3 == refreshl.count && dlabel7 != String(cString:[112,0], encoding: .utf8)!) && (2258426 == refreshl.count)
      repeat {
          var statusf: String! = String(cString: [97,114,116,105,99,108,101,115,0], encoding: .utf8)!
          var e_countH: String! = String(cString: [103,114,101,103,0], encoding: .utf8)!
         currentZ = "\(e_countH.count)"
         statusf.append("\(((String(cString:[89,0], encoding: .utf8)!) == statusf ? statusf.count : statusf.count))")
         e_countH = "\(statusf.count / (Swift.max(statusf.count, 1)))"
         if currentZ.count == 592148 {
            break
         }
      } while (currentZ.count <= 4) && (currentZ.count == 592148)
      repeat {
         handlerk /= Swift.max(Double(currentZ.count & 3), 2)
         if 3085840.0 == handlerk {
            break
         }
      } while (handlerk > Double(dlabel7.count)) && (3085840.0 == handlerk)
      userdata5 -= Float(Int(handlerk))
   }
   while (!avatars.contains { $0 as? Float == userdata5 }) {
      userdata5 /= Swift.max(3, Float(avatars.count))
      break
   }
   repeat {
      userdata5 /= Swift.max(4, Float(Int(userdata5)))
      if 2029082.0 == userdata5 {
         break
      }
   } while (2029082.0 == userdata5) && (3 >= (5 - avatars.count))
     var loginTrue_6z: Bool = true
     let timelabelFile: Double = 2039.0
    var crashedPuts: String! = String(cString: [86,0], encoding: .utf8)!

    return crashedPuts

}





    
    func onNuiTtsVolumeCallback(_ volume: Int32, taskId task_id: UnsafeMutablePointer<CChar>!) {

             recognitionObjectProEnter()


       var index_: String! = String(cString: [109,112,108,97,110,101,0], encoding: .utf8)!
    var systemh: Double = 3.0
    var picturez: Double = 0.0
      systemh += Double(Int(systemh))
       var documentG: Bool = false
       var documenta: [String: Any]! = [String(cString: [112,110,103,0], encoding: .utf8)!:[String(cString: [97,112,112,115,102,108,121,101,114,0], encoding: .utf8)!:[664, 184]]]
       var selected1: Int = 1
         selected1 ^= 3 & selected1
      if 1 >= (1 | documenta.values.count) && documenta.values.count >= 1 {
          var alifastY: String! = String(cString: [98,117,102,102,101,114,0], encoding: .utf8)!
          var namelabelP: String! = String(cString: [102,101,116,99,104,0], encoding: .utf8)!
          var config_: Double = 0.0
          var uploadB: String! = String(cString: [98,105,116,114,97,116,101,115,0], encoding: .utf8)!
         documentG = documentG && selected1 > 68
         alifastY.append("\(uploadB.count)")
         namelabelP.append("\(alifastY.count)")
         config_ += (Double((String(cString:[107,0], encoding: .utf8)!) == uploadB ? uploadB.count : Int(config_)))
      }
      repeat {
          var result1: Int = 1
          var dich: Float = 2.0
         documenta["\(dich)"] = 1 | Int(dich)
         result1 &= 2 >> (Swift.min(4, labs(result1)))
         if documenta.count == 1862910 {
            break
         }
      } while (documenta.count == 1862910) && (1 <= (1 + selected1))
         documenta = ["\(selected1)": selected1]
          var delete_ufV: Bool = true
         documenta = ["\(delete_ufV)": ((documentG ? 2 : 5) | 1)]
         documenta["\(documentG)"] = (1 * (documentG ? 3 : 4))
      repeat {
         documenta = ["\(selected1)": 1]
         if 671969 == documenta.count {
            break
         }
      } while (1 <= (2 - documenta.keys.count) || documenta.keys.count <= 2) && (671969 == documenta.count)
      if documenta.count <= 1 {
          var playingT: Int = 0
          var expireq: [Any]! = [String(cString: [112,97,117,115,101,0], encoding: .utf8)!, String(cString: [120,112,101,114,105,109,101,110,116,97,108,0], encoding: .utf8)!, String(cString: [99,111,108,108,97,116,105,110,103,0], encoding: .utf8)!]
          var timerB: Bool = false
          var iconb: Bool = false
          var time_ono: String! = String(cString: [116,114,97,99,101,114,0], encoding: .utf8)!
         documenta["\(selected1)"] = documenta.values.count << (Swift.min(labs(2), 1))
         playingT |= 1 % (Swift.max(playingT, 3))
         expireq = [playingT]
         timerB = !timerB
         iconb = (!iconb ? timerB : iconb)
         time_ono.append("\(1)")
      }
         documenta = ["\(documenta.count)": (1 | (documentG ? 1 : 5))]
      systemh -= Double(3)

   for _ in 0 ..< 2 {
      systemh /= Swift.max(Double(1), 1)
   }
   for _ in 0 ..< 3 {
      index_ = "\(((String(cString:[112,0], encoding: .utf8)!) == index_ ? index_.count : index_.count))"
   }
    
        debugPrint("——————音频波纹：\(volume)")
    
      index_ = "\(index_.count)"
    }

    
    func onNuiTtsEventCallback(_ event: NuiSdkTtsEvent, taskId taskid: UnsafeMutablePointer<CChar>!, code: Int32) {
       var textB: String! = String(cString: [100,101,118,105,99,101,0], encoding: .utf8)!
    var viiewU: Float = 3.0
      viiewU *= Float(Int(viiewU))

   repeat {
       var dictU: [Any]! = [9096.0]
       var timelabelh: Double = 4.0
       var messagesL: String! = String(cString: [114,101,113,117,115,116,101,114,0], encoding: .utf8)!
       var hengy: Float = 0.0
       var vipN: Float = 3.0
      while (messagesL.contains("\(timelabelh)")) {
         timelabelh -= (Double(messagesL == (String(cString:[71,0], encoding: .utf8)!) ? Int(hengy) : messagesL.count))
         break
      }
         dictU.append(1 >> (Swift.min(5, messagesL.count)))
         vipN -= Float(2)
      for _ in 0 ..< 2 {
         messagesL.append("\(Int(hengy))")
      }
       var collectionK: [String: Any]! = [String(cString: [100,105,115,109,105,115,115,101,115,0], encoding: .utf8)!:800, String(cString: [115,101,99,116,111,114,0], encoding: .utf8)!:224, String(cString: [102,105,114,115,116,0], encoding: .utf8)!:638]
      repeat {
         hengy /= Swift.max(Float(2), 3)
         if hengy == 2709993.0 {
            break
         }
      } while (hengy == 2709993.0) && (3.7 >= (Float(dictU.count) / (Swift.max(6, hengy))) || (hengy / (Swift.max(3.7, 3))) >= 3.95)
          var relationc: Float = 1.0
          var didT: String! = String(cString: [114,111,116,97,116,105,111,110,97,110,103,108,101,0], encoding: .utf8)!
          var holderlabeld: String! = String(cString: [117,110,115,117,112,112,111,114,116,101,100,0], encoding: .utf8)!
         timelabelh *= Double(messagesL.count)
         relationc += Float(2)
         didT.append("\(1 + Int(relationc))")
         holderlabeld.append("\((didT == (String(cString:[82,0], encoding: .utf8)!) ? holderlabeld.count : didT.count))")
      repeat {
         timelabelh *= (Double(messagesL == (String(cString:[103,0], encoding: .utf8)!) ? collectionK.values.count : messagesL.count))
         if timelabelh == 2900558.0 {
            break
         }
      } while (timelabelh == 2900558.0) && (4.3 < (Double(hengy) + timelabelh))
      for _ in 0 ..< 3 {
          var play_: [String: Any]! = [String(cString: [106,117,115,116,105,102,105,101,100,0], encoding: .utf8)!:710, String(cString: [103,101,116,108,97,100,100,114,115,0], encoding: .utf8)!:419, String(cString: [99,111,109,112,101,110,115,97,116,101,100,0], encoding: .utf8)!:972]
          var cancelX: [String: Any]! = [String(cString: [115,97,108,115,97,0], encoding: .utf8)!:253, String(cString: [111,98,116,97,105,110,0], encoding: .utf8)!:16]
          var section7: Float = 0.0
         timelabelh -= Double(Int(vipN))
         play_["\(section7)"] = Int(section7) / (Swift.max(3, 4))
         cancelX = ["\(cancelX.keys.count)": 3 + Int(section7)]
      }
      repeat {
         messagesL.append("\(1 | collectionK.values.count)")
         if messagesL == (String(cString:[101,104,50,120,57,116,105,106,57,0], encoding: .utf8)!) {
            break
         }
      } while (5 < (2 * messagesL.count)) && (messagesL == (String(cString:[101,104,50,120,57,116,105,106,57,0], encoding: .utf8)!))
         hengy *= Float(dictU.count)
         messagesL = "\(3)"
         timelabelh /= Swift.max(5, Double(Int(hengy)))
      repeat {
          var ylabelv: String! = String(cString: [115,111,117,110,100,0], encoding: .utf8)!
         hengy += Float(dictU.count)
         ylabelv.append("\((ylabelv == (String(cString:[71,0], encoding: .utf8)!) ? ylabelv.count : ylabelv.count))")
         if hengy == 4837289.0 {
            break
         }
      } while (hengy == 4837289.0) && (hengy < 3.16)
          var recordingB: Bool = true
          var completionT: String! = String(cString: [97,110,97,108,121,122,105,110,103,0], encoding: .utf8)!
          var qlabeln: Double = 1.0
         timelabelh /= Swift.max(1, (Double((recordingB ? 2 : 2) - Int(timelabelh))))
         recordingB = 69.24 == qlabeln
         completionT = "\(2)"
         qlabeln += Double(1 ^ Int(qlabeln))
      viiewU += Float(messagesL.count)
      if 823433.0 == viiewU {
         break
      }
   } while (823433.0 == viiewU) && (viiewU <= 5.72)
            
       if event == NuiSdkTtsEvent(rawValue: 0) {
           
           do {
               try AVAudioSession.sharedInstance().setCategory(.playback, options: .mixWithOthers)
   if 2.54 < (1.11 + viiewU) {
       var rawingG: String! = String(cString: [114,101,98,97,108,97,110,99,101,0], encoding: .utf8)!
       var headeru: String! = String(cString: [111,112,116,105,111,110,97,108,108,121,0], encoding: .utf8)!
         rawingG.append("\(2 ^ rawingG.count)")
         rawingG.append("\(headeru.count >> (Swift.min(rawingG.count, 3)))")
          var ringp: String! = String(cString: [110,101,119,115,0], encoding: .utf8)!
          var flowI: Double = 2.0
         rawingG.append("\(((String(cString:[89,0], encoding: .utf8)!) == headeru ? headeru.count : rawingG.count))")
         ringp.append("\(Int(flowI))")
         flowI += Double(Int(flowI))
         headeru = "\(2)"
      if !headeru.hasSuffix("\(rawingG.count)") {
          var silenceU: String! = String(cString: [102,114,97,103,0], encoding: .utf8)!
          var collate0: Float = 5.0
          var speakS: String! = String(cString: [115,121,110,99,104,114,111,110,105,122,101,100,0], encoding: .utf8)!
         headeru = "\(1)"
         silenceU = "\(speakS.count)"
         collate0 += Float(speakS.count)
      }
      if headeru.count <= rawingG.count {
         headeru.append("\(headeru.count / 2)")
      }
      textB.append("\(((String(cString:[108,0], encoding: .utf8)!) == textB ? Int(viiewU) : textB.count))")
   }

      textB.append("\(Int(viiewU) ^ 2)")
               try AVAudioSession.sharedInstance().setActive(true)
           } catch {
               print("Failed to set audio session category: \(error)")
           }
           voicePlayer.play()
           self.completionHandler?(.VMineCommon)

       } else if event == NuiSdkTtsEvent(rawValue: 1) || event == NuiSdkTtsEvent(rawValue: 2) || event == NuiSdkTtsEvent(rawValue: 5) {
           if event == NuiSdkTtsEvent(rawValue: 1) {
               
               voicePlayer.drain()
           } else {
               
           }
           if event == NuiSdkTtsEvent(rawValue: 5) {
               stopPlay(false)
               let purchased = utils.nui_tts_get_param("error_msg")
               print(purchased)
           }
       }
    }

    
    func playerDidFinish() {
       var didg: Float = 2.0
    var brief0: Double = 2.0
       var navigation1: Bool = false
       var chuangA: String! = String(cString: [103,101,116,99,0], encoding: .utf8)!
       var lines3: Double = 4.0
         lines3 *= Double(Int(lines3) | 2)
         navigation1 = chuangA.contains("\(navigation1)")
      while (2 >= (3 | chuangA.count) || 2.11 >= (Double(chuangA.count) + lines3)) {
          var browserU: String! = String(cString: [110,105,100,115,0], encoding: .utf8)!
          var detailsI: Double = 0.0
         chuangA = "\(((navigation1 ? 3 : 4) & Int(detailsI)))"
         browserU.append("\(((String(cString:[55,0], encoding: .utf8)!) == browserU ? browserU.count : browserU.count))")
         detailsI += Double(browserU.count)
         break
      }
      while (navigation1) {
         navigation1 = 95.67 == lines3
         break
      }
         chuangA = "\(((navigation1 ? 5 : 5) % (Swift.max(Int(lines3), 7))))"
         navigation1 = !chuangA.hasPrefix("\(lines3)")
          var shared5: String! = String(cString: [108,111,119,99,111,109,112,0], encoding: .utf8)!
          var viiewA: Float = 5.0
         chuangA.append("\(3 & Int(viiewA))")
         shared5.append("\(shared5.count + shared5.count)")
         viiewA *= Float(shared5.count)
         chuangA.append("\(2 % (Swift.max(10, Int(lines3))))")
         lines3 *= Double(3)
      didg /= Swift.max(Float(Int(lines3)), 1)
       var register_ex: Double = 4.0
       var evetQ: Float = 1.0
         register_ex /= Swift.max(2, Double(Int(evetQ)))
       var while_5mK: Double = 3.0
         register_ex *= Double(Int(evetQ))
      for _ in 0 ..< 2 {
          var ismainM: String! = String(cString: [114,97,115,116,101,114,0], encoding: .utf8)!
          var play2: String! = String(cString: [99,98,99,114,0], encoding: .utf8)!
         register_ex *= Double(Int(while_5mK))
         ismainM.append("\(ismainM.count - 1)")
         play2.append("\(3 | play2.count)")
      }
      for _ in 0 ..< 2 {
         register_ex *= Double(1)
      }
       var freeM: Bool = true
       var addW: Bool = false
      didg -= Float(Int(brief0) % 2)

       var edit0: String! = String(cString: [109,98,108,111,111,112,0], encoding: .utf8)!
      if !edit0.hasSuffix(edit0) {
         edit0 = "\(3 + edit0.count)"
      }
          var clearV: Double = 3.0
         edit0 = "\(edit0.count)"
         clearV -= Double(3)
         edit0 = "\(edit0.count + edit0.count)"
      brief0 *= Double(edit0.count % 2)
   if 1.85 <= (brief0 - 3.24) || (didg + 3.24) <= 1.70 {
      didg -= Float(Int(brief0))
   }
        
         stopPlay()
    
    }
}


class NCShow: NSObject {
var remarkBefore_arr: [Any]!
var readGundSheet_dictionary: [String: Any]!



    
    var generateStrExpireList: [Any]!
    var has_Request: Bool = false
    
    private let speechRecognizer = SFSpeechRecognizer(locale: Locale(identifier: "zh-CN"))!
    private var recognitionRequest: SFSpeechAudioBufferRecognitionRequest?
    private var recognitionTask: SFSpeechRecognitionTask?
    private let audioEngine = AVAudioEngine()
    private var voiceCancel: Bool = false
    private var isDetectionEnabled: Bool
    var speakTimer: DispatchSourceTimer?
    private let silenceThreshold: TimeInterval = 3.0
    private let generator = UIImpactFeedbackGenerator(style: .light)

    var resultHandler: ((String) -> Void)?
    var decibelScaleHandler: ((Float) -> Void)?

    init(isDetectionEnabled: Bool = false) {
        self.isDetectionEnabled = isDetectionEnabled
        self.generator.prepare()
    }

@discardableResult
 func userToolDismissArcSpeech(objectSpeeds: Double) -> [Any]! {
    var startW: Double = 1.0
    var playC: String! = String(cString: [115,108,101,101,112,0], encoding: .utf8)!
       var chatlabeln: [Any]! = [220, 542]
       var commont: String! = String(cString: [102,97,105,108,97,98,108,101,0], encoding: .utf8)!
          var strV: Float = 2.0
         commont = "\(chatlabeln.count)"
         strV /= Swift.max(5, Float(Int(strV)))
          var detailsu: Double = 2.0
         commont = "\(3)"
         detailsu /= Swift.max(Double(Int(detailsu) + 3), 5)
      if (chatlabeln.count / (Swift.max(commont.count, 3))) > 2 || 5 > (chatlabeln.count / 2) {
         chatlabeln.append(chatlabeln.count)
      }
      repeat {
         commont.append("\(((String(cString:[106,0], encoding: .utf8)!) == commont ? chatlabeln.count : commont.count))")
         if (String(cString:[106,49,102,119,111,51,54,53,0], encoding: .utf8)!) == commont {
            break
         }
      } while ((String(cString:[106,49,102,119,111,51,54,53,0], encoding: .utf8)!) == commont) && (5 <= (commont.count * chatlabeln.count))
          var avatarD: Float = 1.0
         commont.append("\(chatlabeln.count * 1)")
         avatarD /= Swift.max(2, Float(2 / (Swift.max(Int(avatarD), 5))))
      for _ in 0 ..< 1 {
          var navS: String! = String(cString: [102,111,114,119,97,114,100,0], encoding: .utf8)!
          var choosed: Double = 4.0
          var tabbar7: [String: Any]! = [String(cString: [98,117,98,98,108,101,0], encoding: .utf8)!:92, String(cString: [99,111,110,99,101,97,108,101,100,0], encoding: .utf8)!:897]
         commont = "\(((String(cString:[71,0], encoding: .utf8)!) == commont ? commont.count : chatlabeln.count))"
         navS = "\(Int(choosed) | 2)"
         choosed *= Double(navS.count)
         tabbar7[navS] = navS.count | 3
      }
      playC.append("\(chatlabeln.count * 3)")
       var mineE: String! = String(cString: [101,114,114,108,111,103,0], encoding: .utf8)!
       var bufferi: String! = String(cString: [109,105,115,99,0], encoding: .utf8)!
       var appV: Int = 2
          var itemT: [String: Any]! = [String(cString: [114,111,112,115,116,101,110,0], encoding: .utf8)!:String(cString: [99,105,112,104,101,114,98,121,110,97,109,101,0], encoding: .utf8)!]
          var resetN: [Any]! = [String(cString: [105,95,54,56,0], encoding: .utf8)!, String(cString: [115,116,99,98,0], encoding: .utf8)!, String(cString: [112,97,110,101,115,0], encoding: .utf8)!]
          var rowsT: Float = 3.0
         bufferi = "\(((String(cString:[79,0], encoding: .utf8)!) == bufferi ? appV : bufferi.count))"
         itemT["\(resetN.count)"] = resetN.count >> (Swift.min(labs(2), 2))
         rowsT -= Float(resetN.count)
       var pathV: [Any]! = [482, 133]
      repeat {
         bufferi.append("\(pathV.count)")
         if (String(cString:[109,50,101,120,118,104,115,55,99,119,0], encoding: .utf8)!) == bufferi {
            break
         }
      } while ((String(cString:[109,50,101,120,118,104,115,55,99,119,0], encoding: .utf8)!) == bufferi) && ((bufferi.count - pathV.count) <= 2 && 5 <= (pathV.count - 2))
          var cancelz: Float = 2.0
          var indicesv: String! = String(cString: [118,102,105,108,116,101,114,0], encoding: .utf8)!
          var numberlabelf: String! = String(cString: [105,100,120,0], encoding: .utf8)!
         pathV = [indicesv.count]
         cancelz -= Float(3 - Int(cancelz))
         indicesv = "\(Int(cancelz))"
         numberlabelf.append("\(Int(cancelz) | 2)")
         mineE.append("\(((String(cString:[116,0], encoding: .utf8)!) == bufferi ? bufferi.count : pathV.count))")
          var main_mz: Float = 5.0
          var order2: [Any]! = [String(cString: [114,107,109,112,112,0], encoding: .utf8)!, String(cString: [112,114,105,111,114,105,116,121,113,0], encoding: .utf8)!, String(cString: [99,108,97,117,115,101,115,0], encoding: .utf8)!]
          var completione: String! = String(cString: [115,97,109,112,108,101,114,101,100,117,99,116,105,111,110,0], encoding: .utf8)!
         pathV.append(bufferi.count & 3)
         main_mz -= Float(order2.count % (Swift.max(9, completione.count)))
         order2 = [order2.count + Int(main_mz)]
         completione.append("\(Int(main_mz) << (Swift.min(completione.count, 5)))")
          var callX: Double = 4.0
         appV += pathV.count | Int(callX)
      while (bufferi.count < pathV.count) {
         pathV = [((String(cString:[98,0], encoding: .utf8)!) == mineE ? mineE.count : bufferi.count)]
         break
      }
       var urlz: Bool = true
      startW -= Double(Int(startW))
    var  gemfileSetupdNoalloc = [Any]()

    return gemfileSetupdNoalloc

}






    
    func startRecording() {

             userToolDismissArcSpeech(objectSpeeds:3447.0)


        
        generator.impactOccurred()
        audioEngine.stop()
        audioEngine.reset()

        DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) { [weak self] in
            
            guard let self = self else { return }

            checkAliToken()
            
            if let recognitionTask = self.recognitionTask {
                recognitionTask.cancel()
                self.recognitionTask = nil
            }

            self.voiceCancel = false
            
            let sublyout = AVAudioSession.sharedInstance()
            try! sublyout.setCategory(.record, mode: .measurement, options: .duckOthers)
            try! sublyout.setActive(true, options: .notifyOthersOnDeactivation)

            self.recognitionRequest = SFSpeechAudioBufferRecognitionRequest()

            let param = self.audioEngine.inputNode

            guard let recognitionRequest = self.recognitionRequest else { fatalError("Unable to create a SFSpeechAudioBufferRecognitionRequest object") }

            recognitionRequest.shouldReportPartialResults = true
            
            self.recognitionTask = self.speechRecognizer.recognitionTask(with: recognitionRequest) { result, error in
                var size_hsButton = false
                
                if let result = result {
                    
                    if self.isDetectionEnabled {
                        self.netSearchInputIdleRegionSketch()
                        self.minimumMeasurementChooseFoundationSpeedPlay()
                    }
                    print("——————说话中: \(result.bestTranscription.formattedString)")
                    size_hsButton = result.isFinal
                    NotificationCenter.default.post(name: NSNotification.Name("VoiceMessageNotificationName"), object: result.bestTranscription.formattedString)
                }

                if error != nil || size_hsButton {
                    self.audioEngine.stop()
                    param.removeTap(onBus: 0)

                    self.recognitionRequest = nil
                    self.recognitionTask = nil

                    if let result = result {
                        if self.voiceCancel == false {
                            self.resultHandler?(result.bestTranscription.formattedString)
                        }
                    }
                }
            }

            let resources = param.outputFormat(forBus: 0)
            param.installTap(onBus: 0, bufferSize: 1024, format: resources) { (buffer: AVAudioPCMBuffer, when: AVAudioTime) in
                self.recognitionRequest?.append(buffer)
                
                
                guard let channelData = buffer.floatChannelData else { return }
                
                
                let navgationView = vDSP_Length(buffer.frameLength)
                var desc: Float = 0
                vDSP_rmsqv(channelData[0], 1, &desc, navgationView)
                
                
                let instance = 20 * log10(desc)
                
                
                DispatchQueue.main.async {
                    
                    let socket = 1.0 + CGFloat((instance + 50) / 50.0)
                    let q_title = max(1.0, min(socket, 1.5))
                    
                    if q_title > 1.0 {
                        self.decibelScaleHandler?(Float(q_title))
                    }
                }
            }
            
            self.audioEngine.prepare()
            try! self.audioEngine.start()
        }
        
    }

@discardableResult
 func insertEachElementSumLevelPrompt(clearAudio: Int, confirmPicked: Bool, checkPress: Float) -> String! {
    var numberlabelV: String! = String(cString: [100,101,115,99,101,110,116,0], encoding: .utf8)!
    var notification1: Double = 0.0
   if !numberlabelV.contains("\(notification1)") {
      numberlabelV = "\(((String(cString:[114,0], encoding: .utf8)!) == numberlabelV ? Int(notification1) : numberlabelV.count))"
   }
   if numberlabelV.count <= 3 {
       var shout: String! = String(cString: [102,97,99,116,111,114,97,98,108,101,0], encoding: .utf8)!
       var homep: Float = 2.0
       var long_3w: String! = String(cString: [112,97,105,110,116,105,110,103,0], encoding: .utf8)!
       var remarkH: String! = String(cString: [120,105,112,104,0], encoding: .utf8)!
       var userK: Double = 2.0
       var gif2: String! = String(cString: [115,99,97,108,101,115,0], encoding: .utf8)!
      if 2.34 > (homep / (Swift.max(Float(remarkH.count), 10))) {
          var qlabelG: Float = 1.0
          var picturev: [Any]! = [String(cString: [101,109,111,114,121,0], encoding: .utf8)!, String(cString: [114,101,118,111,107,101,0], encoding: .utf8)!]
         homep -= Float(Int(userK) & 2)
         qlabelG *= Float(picturev.count)
         picturev = [picturev.count]
      }
      repeat {
         gif2 = "\(((String(cString:[98,0], encoding: .utf8)!) == gif2 ? long_3w.count : gif2.count))"
         if 1674190 == gif2.count {
            break
         }
      } while (long_3w == gif2) && (1674190 == gif2.count)
      for _ in 0 ..< 1 {
          var alamofirea: Int = 3
          var numberD: String! = String(cString: [100,97,114,119,105,110,0], encoding: .utf8)!
          var sharedu: [String: Any]! = [String(cString: [104,97,100,97,109,97,114,100,0], encoding: .utf8)!:543, String(cString: [112,108,97,99,101,115,0], encoding: .utf8)!:599, String(cString: [112,114,101,118,105,101,119,112,112,0], encoding: .utf8)!:515]
         userK /= Swift.max(Double(long_3w.count & 2), 4)
         alamofirea -= 1 - alamofirea
         numberD.append("\(alamofirea + 2)")
         sharedu = [numberD: 2 << (Swift.min(4, labs(alamofirea)))]
      }
         userK /= Swift.max(4, Double(2))
         shout.append("\(Int(userK))")
         userK *= Double(3)
         homep -= Float(Int(homep))
      repeat {
          var commentv: String! = String(cString: [99,111,110,99,97,116,101,110,97,116,101,0], encoding: .utf8)!
         gif2 = "\(Int(userK) ^ remarkH.count)"
         commentv.append("\(1)")
         if (String(cString:[54,106,102,116,52,107,54,120,106,0], encoding: .utf8)!) == gif2 {
            break
         }
      } while ((String(cString:[54,106,102,116,52,107,54,120,106,0], encoding: .utf8)!) == gif2) && ((userK / 2.5) >= 5.12 && (2.5 / (Swift.max(9, userK))) >= 3.9)
         gif2.append("\(shout.count % (Swift.max(1, Int(homep))))")
      if !gif2.hasPrefix("\(userK)") {
         userK *= Double(1)
      }
      for _ in 0 ..< 2 {
         remarkH.append("\(Int(userK))")
      }
      if 1.89 < (Double(Int(userK) / 5)) {
         homep *= Float(remarkH.count)
      }
      repeat {
         remarkH.append("\(remarkH.count * 2)")
         if 2143329 == remarkH.count {
            break
         }
      } while (3 == (1 - Int(userK)) && (Int(userK) - remarkH.count) == 1) && (2143329 == remarkH.count)
          var class_h81: String! = String(cString: [115,105,108,101,110,99,101,0], encoding: .utf8)!
         shout = "\(Int(homep))"
         class_h81 = "\(class_h81.count / 1)"
      numberlabelV.append("\(2 + Int(notification1))")
   }
   if notification1 >= 2.18 {
       var recordingh: Int = 2
       var navgationw: Bool = false
       var detect4: [Any]! = [String(cString: [107,101,114,110,101,108,0], encoding: .utf8)!, String(cString: [101,120,105,115,116,115,0], encoding: .utf8)!]
         detect4.append((recordingh >> (Swift.min(2, labs((navgationw ? 4 : 1))))))
      repeat {
         detect4.append((detect4.count | (navgationw ? 1 : 1)))
         if 2625245 == detect4.count {
            break
         }
      } while (2 >= detect4.count && 1 >= (detect4.count + 2)) && (2625245 == detect4.count)
         detect4.append(2)
      while ((detect4.count - 5) < 2 || 5 < detect4.count) {
         navgationw = !navgationw && detect4.count > 21
         break
      }
         navgationw = 51 > recordingh && !navgationw
      if navgationw {
         navgationw = recordingh >= 86
      }
          var changes: String! = String(cString: [118,116,97,103,0], encoding: .utf8)!
          var liholderlabeld: Double = 1.0
          var model8: Double = 3.0
         navgationw = recordingh < detect4.count
         changes = "\(changes.count | Int(liholderlabeld))"
         liholderlabeld *= Double(changes.count)
         model8 *= Double(3 << (Swift.min(labs(Int(liholderlabeld)), 4)))
          var contents7: [String: Any]! = [String(cString: [114,101,116,114,105,101,114,0], encoding: .utf8)!:701, String(cString: [119,111,114,107,108,111,97,100,0], encoding: .utf8)!:95]
         recordingh /= Swift.max(2, 1 + contents7.count)
      repeat {
         detect4.append(1)
         if detect4.count == 516986 {
            break
         }
      } while (detect4.count == 516986) && (5 < (recordingh ^ 3) || (detect4.count ^ recordingh) < 3)
      notification1 /= Swift.max(1, Double(2))
   }
       var urle: String! = String(cString: [108,97,121,101,114,0], encoding: .utf8)!
       var removeY: Float = 4.0
       var int_7jG: Float = 2.0
       var switch_5pj: Float = 4.0
      for _ in 0 ..< 2 {
         int_7jG -= Float(Int(int_7jG) - Int(switch_5pj))
      }
       var judges: Bool = false
         judges = urle.hasSuffix("\(removeY)")
      while (1.42 < int_7jG) {
         int_7jG *= Float(1 % (Swift.max(Int(switch_5pj), 8)))
         break
      }
         judges = switch_5pj == 4.8
         removeY += (Float(Int(switch_5pj) * (judges ? 5 : 4)))
      if judges {
         judges = 54.95 < removeY
      }
       var bonko: Double = 4.0
       var speechW: Double = 3.0
      notification1 *= (Double((String(cString:[49,0], encoding: .utf8)!) == urle ? Int(notification1) : urle.count))
   return numberlabelV

}






    func stopRecording() {

             insertEachElementSumLevelPrompt(clearAudio:8618, confirmPicked:false, checkPress:6456.0)


       var constraint9: String! = String(cString: [116,101,114,109,105,110,97,116,105,111,110,0], encoding: .utf8)!
    var beginH: Double = 2.0
   repeat {
       var zhidingesu: [Any]! = [438, 426, 223]
       var n_managerG: [Any]! = [606, 988, 948]
         n_managerG.append(zhidingesu.count)
      while (4 == (n_managerG.count * 2)) {
         n_managerG = [n_managerG.count]
         break
      }
         zhidingesu.append(zhidingesu.count >> (Swift.min(5, n_managerG.count)))
      if 4 < (zhidingesu.count & n_managerG.count) && 4 < (zhidingesu.count & n_managerG.count) {
          var createG: Float = 3.0
          var rawingC: Bool = false
          var popupP: String! = String(cString: [101,114,114,110,111,0], encoding: .utf8)!
          var zhidingesI: String! = String(cString: [116,114,105,103,103,101,114,101,100,0], encoding: .utf8)!
          var desce: [String: Any]! = [String(cString: [99,97,116,101,103,111,114,105,115,101,100,0], encoding: .utf8)!:String(cString: [108,97,110,103,117,97,103,101,115,0], encoding: .utf8)!, String(cString: [104,101,118,99,100,101,99,0], encoding: .utf8)!:String(cString: [115,116,114,108,101,110,0], encoding: .utf8)!, String(cString: [98,97,99,107,103,114,111,117,110,100,115,0], encoding: .utf8)!:String(cString: [97,112,105,115,0], encoding: .utf8)!]
         n_managerG.append(n_managerG.count | 2)
         createG += Float(desce.values.count + 2)
         rawingC = zhidingesI.hasSuffix("\(rawingC)")
         popupP = "\(((rawingC ? 1 : 3)))"
         zhidingesI.append("\(desce.count)")
      }
      repeat {
          var downloadC: Float = 1.0
          var namey: Double = 1.0
          var collate4: String! = String(cString: [115,104,111,119,105,110,103,0], encoding: .utf8)!
         n_managerG.append(Int(namey) % 1)
         downloadC *= (Float(collate4 == (String(cString:[87,0], encoding: .utf8)!) ? collate4.count : Int(downloadC)))
         namey -= Double(collate4.count + 3)
         if n_managerG.count == 1730477 {
            break
         }
      } while (n_managerG.count == 1730477) && (2 > (n_managerG.count * zhidingesu.count) && (n_managerG.count * zhidingesu.count) > 2)
      while (5 > zhidingesu.count) {
         n_managerG = [zhidingesu.count * n_managerG.count]
         break
      }
      constraint9 = "\(constraint9.count / 3)"
      if 2418485 == constraint9.count {
         break
      }
   } while (2418485 == constraint9.count) && ((4 + constraint9.count) <= 5 && (constraint9.count - Int(beginH)) <= 4)

      beginH *= Double(Int(beginH) ^ 3)
        
        generator.impactOccurred()
        
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) { [weak self] in
            guard let self = self else { return }

   if (5.4 * beginH) == 2.13 {
      beginH -= Double(Int(beginH) << (Swift.min(labs(2), 3)))
   }
            self.netSearchInputIdleRegionSketch()
   while (5.53 < (Double(constraint9.count) - beginH) || (Double(constraint9.count) - beginH) < 5.53) {
      constraint9.append("\((constraint9 == (String(cString:[76,0], encoding: .utf8)!) ? constraint9.count : Int(beginH)))")
      break
   }
            self.audioEngine.stop()
            self.recognitionRequest?.endAudio()
            let sublyout = AVAudioSession.sharedInstance()
            try! sublyout.setCategory(.playback, mode: .default)
            try! sublyout.setActive(true, options: .notifyOthersOnDeactivation)
        }
        
    }

@discardableResult
 func requestRenewalSuccessPageSenderRecord(rnewsApply: Int, recordingRecording: Int) -> Double {
    var sourcev: Bool = false
    var nicknameE: Bool = false
   for _ in 0 ..< 3 {
      nicknameE = (!sourcev ? nicknameE : sourcev)
   }
   if !nicknameE {
       var long_h8O: String! = String(cString: [115,116,114,105,107,101,116,104,114,111,117,103,104,115,0], encoding: .utf8)!
       var sublyoutR: String! = String(cString: [101,120,116,114,97,100,97,116,97,99,111,110,102,105,103,0], encoding: .utf8)!
       var jnews1: [String: Any]! = [String(cString: [114,102,102,116,102,0], encoding: .utf8)!:720, String(cString: [118,105,97,98,108,101,0], encoding: .utf8)!:441, String(cString: [100,97,105,0], encoding: .utf8)!:335]
       var layouth: [Any]! = [String(cString: [115,112,105,108,108,115,105,122,101,0], encoding: .utf8)!, String(cString: [111,110,108,105,110,101,115,0], encoding: .utf8)!]
       var preferredO: [Any]! = [String(cString: [105,110,116,101,103,114,97,108,0], encoding: .utf8)!, String(cString: [115,99,114,111,108,108,97,98,108,101,0], encoding: .utf8)!]
      for _ in 0 ..< 3 {
         jnews1[long_h8O] = sublyoutR.count
      }
         preferredO.append(sublyoutR.count)
       var chuang4: Int = 3
      for _ in 0 ..< 3 {
         long_h8O = "\(chuang4 ^ 2)"
      }
          var self_ps7: Bool = true
         layouth.append(preferredO.count)
         self_ps7 = !self_ps7
          var leftc: Float = 5.0
         preferredO = [(long_h8O == (String(cString:[74,0], encoding: .utf8)!) ? jnews1.count : long_h8O.count)]
         leftc += Float(1)
         layouth = [preferredO.count | chuang4]
          var successN: [String: Any]! = [String(cString: [98,117,102,102,101,114,113,117,101,117,101,0], encoding: .utf8)!:110, String(cString: [104,113,112,101,108,0], encoding: .utf8)!:442, String(cString: [118,97,114,105,97,110,99,101,120,0], encoding: .utf8)!:865]
          var resourceso: String! = String(cString: [102,111,114,109,0], encoding: .utf8)!
          var baseF: Float = 1.0
         long_h8O.append("\(1)")
         successN = [resourceso: 1 / (Swift.max(4, Int(baseF)))]
         resourceso = "\(resourceso.count % (Swift.max(9, successN.values.count)))"
         baseF *= Float(resourceso.count % 2)
      repeat {
          var audioi: String! = String(cString: [104,119,117,112,108,111,97,100,0], encoding: .utf8)!
          var otheru: Double = 3.0
         chuang4 <<= Swift.min(labs(3), 3)
         audioi = "\(audioi.count)"
         otheru -= Double(audioi.count)
         if chuang4 == 952147 {
            break
         }
      } while (preferredO.contains { $0 as? Int == chuang4 }) && (chuang4 == 952147)
         chuang4 -= 2 & jnews1.keys.count
          var nameo: String! = String(cString: [115,115,116,104,114,101,115,104,0], encoding: .utf8)!
          var launchD: Double = 3.0
         layouth = [1]
         nameo.append("\(((String(cString:[70,0], encoding: .utf8)!) == nameo ? Int(launchD) : nameo.count))")
         launchD += Double(3 & nameo.count)
      if 1 == (chuang4 * 1) && (1 * chuang4) == 2 {
          var dlabel_: String! = String(cString: [97,99,116,97,98,0], encoding: .utf8)!
          var scrollB: Double = 1.0
          var placeholder5: [Any]! = [UILabel(frame:CGRect.zero)]
         sublyoutR = "\(chuang4)"
         dlabel_.append("\(Int(scrollB) - placeholder5.count)")
         scrollB *= Double(dlabel_.count | 3)
         placeholder5 = [placeholder5.count << (Swift.min(labs(3), 5))]
      }
      for _ in 0 ..< 3 {
         layouth = [2 >> (Swift.min(labs(chuang4), 1))]
      }
         chuang4 += preferredO.count << (Swift.min(labs(2), 5))
       var weak_jV: Double = 3.0
       var resourcest: Double = 4.0
      sourcev = (layouth.count >> (Swift.min(4, preferredO.count))) <= 43
   }
       var recordingP: [String: Any]! = [String(cString: [99,97,110,110,111,116,0], encoding: .utf8)!:353, String(cString: [98,97,110,0], encoding: .utf8)!:365, String(cString: [105,110,116,101,114,112,111,108,97,116,111,114,0], encoding: .utf8)!:977]
         recordingP["\(recordingP.keys.count)"] = recordingP.keys.count / 1
         recordingP = ["\(recordingP.values.count)": recordingP.values.count]
       var didK: Double = 2.0
       var liholderlabela: Double = 4.0
      sourcev = (((nicknameE ? 66 : recordingP.count) >> (Swift.min(recordingP.count, 3))) > 66)
   for _ in 0 ..< 1 {
      sourcev = (!nicknameE ? sourcev : nicknameE)
   }
     var flowWeixinlabel: String! = String(cString: [98,101,104,105,110,100,0], encoding: .utf8)!
     var audioClick: Double = 6700.0
     var keywordsVerity: Bool = false
    var cfhdSiffTmmbr:Double = 0
    audioClick *= 85
    cfhdSiffTmmbr -= Double(audioClick)
    keywordsVerity = true
    cfhdSiffTmmbr += Double(keywordsVerity ? 87 : 60)

    return cfhdSiffTmmbr

}






    
    private func netSearchInputIdleRegionSketch() {

             requestRenewalSuccessPageSenderRecord(rnewsApply:8578, recordingRecording:4913)


       var audioY: [String: Any]! = [String(cString: [100,97,118,115,0], encoding: .utf8)!:163, String(cString: [114,99,111,110,0], encoding: .utf8)!:428, String(cString: [110,111,110,98,108,111,99,107,0], encoding: .utf8)!:354]
    var relationj: String! = String(cString: [115,108,97,110,116,0], encoding: .utf8)!
   while ((relationj.count - 3) <= 5 && 2 <= (relationj.count - 3)) {
      audioY = ["\(audioY.keys.count)": audioY.keys.count / (Swift.max(3, 3))]
      break
   }
   for _ in 0 ..< 1 {
       var unselectedX: String! = String(cString: [117,100,112,0], encoding: .utf8)!
       var detaillabel_: Bool = false
       var qnewsB: String! = String(cString: [108,101,97,121,0], encoding: .utf8)!
       var historyZ: String! = String(cString: [122,105,103,122,97,103,0], encoding: .utf8)!
       var appi: String! = String(cString: [117,110,109,97,112,102,105,108,101,0], encoding: .utf8)!
         qnewsB.append("\(3 + historyZ.count)")
         appi.append("\(qnewsB.count / (Swift.max(appi.count, 7)))")
       var typelabelt: String! = String(cString: [114,101,115,117,109,101,0], encoding: .utf8)!
          var hengb: String! = String(cString: [97,117,116,111,105,110,99,114,101,109,101,110,116,0], encoding: .utf8)!
          var failed1: String! = String(cString: [99,111,109,98,0], encoding: .utf8)!
          var alamofireS: Bool = true
         detaillabel_ = typelabelt.count > 25 || !detaillabel_
         hengb.append("\(((alamofireS ? 2 : 4) + failed1.count))")
         failed1 = "\(3)"
          var pickere: String! = String(cString: [115,119,114,101,115,97,109,112,108,101,0], encoding: .utf8)!
         appi = "\(1)"
         pickere.append("\(2)")
         detaillabel_ = !historyZ.hasSuffix("\(detaillabel_)")
         unselectedX = "\(((detaillabel_ ? 5 : 4) % (Swift.max(2, 9))))"
         detaillabel_ = !detaillabel_ || qnewsB.count < 98
         detaillabel_ = unselectedX == typelabelt
      while (appi.count <= unselectedX.count) {
         appi = "\(((detaillabel_ ? 1 : 3) % (Swift.max(9, appi.count))))"
         break
      }
         historyZ = "\((qnewsB.count * (detaillabel_ ? 2 : 5)))"
      if qnewsB.count < 3 && historyZ.count < 3 {
          var navgationX: Int = 4
          var layout1: Double = 4.0
          var baseb: Int = 1
          var valueW: String! = String(cString: [110,112,111,105,110,116,115,0], encoding: .utf8)!
          var mode1: Float = 0.0
         qnewsB = "\(Int(mode1) % (Swift.max(qnewsB.count, 7)))"
         navgationX += 1
         layout1 /= Swift.max(2, Double(baseb & valueW.count))
         baseb >>= Swift.min(labs(navgationX), 1)
         valueW.append("\(3)")
         mode1 += Float(3)
      }
         appi.append("\(typelabelt.count | 3)")
      if 1 > typelabelt.count {
         detaillabel_ = !qnewsB.hasPrefix("\(detaillabel_)")
      }
          var pictureV: [Any]! = [304, 773, 589]
         detaillabel_ = unselectedX == qnewsB
         pictureV.append(pictureV.count)
      relationj = "\((relationj == (String(cString:[112,0], encoding: .utf8)!) ? relationj.count : (detaillabel_ ? 3 : 3)))"
   }

   repeat {
      relationj.append("\(relationj.count | 1)")
      if (String(cString:[101,95,107,120,0], encoding: .utf8)!) == relationj {
         break
      }
   } while (2 <= (relationj.count & audioY.count) || (2 & relationj.count) <= 1) && ((String(cString:[101,95,107,120,0], encoding: .utf8)!) == relationj)
   repeat {
      audioY = ["\(audioY.keys.count)": audioY.count ^ 3]
      if 1945423 == audioY.count {
         break
      }
   } while (audioY.keys.count > 1) && (1945423 == audioY.count)

        speakTimer?.cancel()
        speakTimer = nil
    }


    private func minimumMeasurementChooseFoundationSpeedPlay() {
       var nickname9: Bool = true
    var aidaa: Double = 5.0
    var attributesm: Double = 4.0
   if nickname9 {
      aidaa -= Double(1 << (Swift.min(labs(Int(aidaa)), 5)))
   }

      nickname9 = !nickname9

       var logoZ: String! = String(cString: [114,101,99,111,109,112,117,116,101,0], encoding: .utf8)!
       var rmblabela: Int = 1
       var rawingX: Int = 5
      if 3 == rawingX {
         rmblabela %= Swift.max(3, 1)
      }
       var utilsh: String! = String(cString: [112,114,111,99,101,115,115,111,114,0], encoding: .utf8)!
       var backB: String! = String(cString: [114,101,109,111,118,101,100,0], encoding: .utf8)!
         rawingX ^= utilsh.count + 2
      repeat {
          var attributesy: String! = String(cString: [101,118,101,110,108,121,0], encoding: .utf8)!
          var nextS: Float = 1.0
          var commoni: Double = 4.0
          var taskD: Bool = false
          var speechA: Double = 1.0
         backB.append("\(((taskD ? 5 : 1) / (Swift.max(rawingX, 10))))")
         attributesy.append("\(((String(cString:[50,0], encoding: .utf8)!) == attributesy ? attributesy.count : Int(speechA)))")
         nextS *= Float(Int(nextS) - 2)
         commoni -= Double(1)
         taskD = !attributesy.hasSuffix("\(speechA)")
         if backB.count == 1431715 {
            break
         }
      } while (utilsh != backB) && (backB.count == 1431715)
       var alamofireu: Int = 4
       var screenV: Int = 5
      while (5 >= (1 + rawingX)) {
          var zhidinges2: String! = String(cString: [101,120,99,101,101,100,115,0], encoding: .utf8)!
          var convertedi: String! = String(cString: [99,111,109,112,101,110,115,97,116,105,111,110,0], encoding: .utf8)!
          var ring6: Double = 4.0
         rawingX ^= Int(ring6)
         zhidinges2.append("\(zhidinges2.count + convertedi.count)")
         convertedi.append("\(zhidinges2.count - convertedi.count)")
         ring6 *= Double(zhidinges2.count)
         break
      }
         screenV *= screenV
          var amountt: [String: Any]! = [String(cString: [115,119,105,99,104,0], encoding: .utf8)!:String(cString: [102,114,111,122,101,110,0], encoding: .utf8)!]
          var requestZ: [String: Any]! = [String(cString: [97,112,112,114,101,99,97,116,105,111,110,104,111,117,114,115,0], encoding: .utf8)!:String(cString: [116,105,109,101,108,105,110,101,115,0], encoding: .utf8)!, String(cString: [115,104,117,102,102,108,101,112,108,97,110,101,115,0], encoding: .utf8)!:String(cString: [97,116,111,109,105,99,115,0], encoding: .utf8)!, String(cString: [114,101,99,111,110,102,105,103,117,114,97,116,105,111,110,0], encoding: .utf8)!:String(cString: [109,118,115,101,116,0], encoding: .utf8)!]
          var recognition2: [Any]! = [57, 663, 784]
         rawingX /= Swift.max(3, screenV)
         amountt = ["\(requestZ.count)": recognition2.count]
         requestZ = ["\(requestZ.values.count)": requestZ.count]
         recognition2 = [amountt.values.count / (Swift.max(requestZ.count, 5))]
      for _ in 0 ..< 1 {
          var pickedM: Double = 0.0
          var p_heightL: [String: Any]! = [String(cString: [105,110,105,0], encoding: .utf8)!:String(cString: [99,111,109,112,108,101,116,101,0], encoding: .utf8)!, String(cString: [100,105,121,102,112,0], encoding: .utf8)!:String(cString: [99,111,114,114,101,108,97,116,101,0], encoding: .utf8)!, String(cString: [105,110,105,116,105,97,116,101,100,0], encoding: .utf8)!:String(cString: [117,110,98,97,110,0], encoding: .utf8)!]
          var purchaseV: Double = 1.0
          var btnV: String! = String(cString: [109,97,120,120,0], encoding: .utf8)!
          var accountlabelR: Float = 1.0
         logoZ.append("\(2 % (Swift.max(10, btnV.count)))")
         pickedM += Double(2 >> (Swift.min(labs(Int(accountlabelR)), 3)))
         p_heightL = ["\(p_heightL.keys.count)": 2]
         purchaseV -= Double(Int(accountlabelR) % (Swift.max(p_heightL.keys.count, 10)))
         btnV.append("\(Int(accountlabelR))")
      }
      attributesm *= (Double((nickname9 ? 5 : 1) - 2))
        if speakTimer == nil {
            speakTimer?.cancel()
   while (2.41 < aidaa || (aidaa * 2.41) < 3.25) {
      nickname9 = aidaa <= 68.57
      break
   }
            speakTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.main)
      aidaa /= Swift.max(Double(Int(aidaa) >> (Swift.min(1, labs(2)))), 5)
            speakTimer?.schedule(deadline: .now() + silenceThreshold, repeating: .never)
            speakTimer?.setEventHandler { [weak self] in
   while (nickname9) {
      aidaa += Double(1 << (Swift.min(labs(Int(aidaa)), 5)))
      break
   }
                self?.stopRecording()
            }
            speakTimer?.resume()
        }
    }


    func cancelRecording() {
       var networkr: String! = String(cString: [98,101,105,103,110,101,116,0], encoding: .utf8)!
    var verifyG: String! = String(cString: [103,97,112,0], encoding: .utf8)!
    var reusablel: Double = 3.0
      networkr = "\(1)"
       var contentsf: String! = String(cString: [101,118,97,108,0], encoding: .utf8)!
       var speedsK: String! = String(cString: [115,110,97,112,115,104,111,116,116,101,114,0], encoding: .utf8)!
         speedsK.append("\(speedsK.count - contentsf.count)")
         speedsK = "\(speedsK.count >> (Swift.min(3, contentsf.count)))"
         speedsK.append("\(contentsf.count)")
         contentsf = "\(contentsf.count >> (Swift.min(4, speedsK.count)))"
      if contentsf.contains(speedsK) {
         contentsf.append("\(3 << (Swift.min(3, contentsf.count)))")
      }
      while (4 <= contentsf.count) {
         contentsf.append("\(contentsf.count)")
         break
      }
      networkr.append("\(verifyG.count)")

       var recognizeru: [Any]! = [984, 196]
       var handler9: String! = String(cString: [97,100,100,114,101,115,115,101,115,0], encoding: .utf8)!
       var imagesC: [String: Any]! = [String(cString: [100,101,99,111,109,112,114,101,115,115,105,111,110,0], encoding: .utf8)!:748, String(cString: [109,111,111,102,0], encoding: .utf8)!:992]
         handler9.append("\(handler9.count)")
         imagesC[handler9] = recognizeru.count
         imagesC = ["\(imagesC.keys.count)": imagesC.keys.count << (Swift.min(labs(3), 2))]
       var hengA: String! = String(cString: [99,111,110,102,105,103,117,114,97,116,105,111,110,0], encoding: .utf8)!
       var resourcesU: String! = String(cString: [118,97,108,105,100,97,116,101,0], encoding: .utf8)!
       var jsonJ: String! = String(cString: [99,111,110,116,105,110,117,101,100,0], encoding: .utf8)!
       var promptB: String! = String(cString: [110,99,104,117,110,107,0], encoding: .utf8)!
      networkr.append("\(handler9.count + 1)")
   repeat {
      verifyG.append("\(3 - verifyG.count)")
      if verifyG == (String(cString:[115,97,115,112,98,99,52,106,109,118,0], encoding: .utf8)!) {
         break
      }
   } while (verifyG == (String(cString:[115,97,115,112,98,99,52,106,109,118,0], encoding: .utf8)!)) && (5 >= (verifyG.count ^ 1) || 1.29 >= (reusablel - Double(verifyG.count)))
        
        voiceCancel = true
   if 1.52 == (reusablel + 1.26) {
      reusablel -= Double(Int(reusablel))
   }
       var confirmD: Int = 4
       var create2: String! = String(cString: [118,109,110,99,0], encoding: .utf8)!
      if 5 >= (confirmD + 4) || 3 >= (4 + confirmD) {
         confirmD -= 2 - confirmD
      }
         create2.append("\(2)")
         create2.append("\(confirmD)")
          var userdataH: Int = 2
          var code2: [Any]! = [String(cString: [98,105,110,107,0], encoding: .utf8)!, String(cString: [99,111,111,114,100,105,110,97,116,101,115,0], encoding: .utf8)!, String(cString: [118,101,114,105,102,105,101,100,0], encoding: .utf8)!]
          var dids: String! = String(cString: [114,101,102,101,114,101,110,99,101,100,0], encoding: .utf8)!
         confirmD <<= Swift.min(4, create2.count)
         userdataH -= 3 >> (Swift.min(labs(userdataH), 4))
         code2 = [2 + dids.count]
         dids = "\(userdataH)"
         create2.append("\(confirmD + 1)")
      if (confirmD + 5) == 1 && (confirmD + create2.count) == 5 {
         confirmD >>= Swift.min(labs(1), 2)
      }
      reusablel += Double(create2.count)
        stopRecording()
    }
    
}
