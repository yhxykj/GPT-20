
import Foundation

import UIKit

class EBBaseCell: UICollectionViewCell {
private var tableItemsDictionary: [String: Any]?
private var observationsGifFreeList: [Any]!
var alamofireList_map: [String: Any]?




    @IBOutlet weak var titlelabel: UILabel!
    @IBOutlet weak var backImage: UIImageView!

@discardableResult
 func handyTerminateCompressionMultipleResourceNeverButton() -> UIButton! {
    var true_rg2: String! = String(cString: [113,108,111,103,115,0], encoding: .utf8)!
    var style7: Bool = false
      true_rg2.append("\(true_rg2.count / 3)")
      style7 = ((true_rg2.count + (!style7 ? 22 : true_rg2.count)) == 22)
      style7 = (67 <= ((!style7 ? true_rg2.count : 67) % (Swift.max(true_rg2.count, 4))))
     let fixedHanding: UIButton! = UIButton()
     let size_qHttp: String! = String(cString: [99,111,110,116,111,117,114,115,0], encoding: .utf8)!
     var generatePurchased: UILabel! = UILabel(frame:CGRect(x: 224, y: 404, width: 0, height: 0))
    var sumfWebmids = UIButton(frame:CGRect.zero)
    sumfWebmids.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    sumfWebmids.alpha = 0.5
    sumfWebmids.frame = CGRect(x: 218, y: 79, width: 0, height: 0)
    sumfWebmids.setImage(UIImage(named:String(cString: [105,109,97,103,101,115,0], encoding: .utf8)!), for: .normal)
    sumfWebmids.setTitle("", for: .normal)
    sumfWebmids.setBackgroundImage(UIImage(named:String(cString: [110,117,109,98,101,114,108,97,98,101,108,0], encoding: .utf8)!), for: .normal)
    sumfWebmids.titleLabel?.font = UIFont.systemFont(ofSize:15)
    fixedHanding.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 1)
    fixedHanding.alpha = 1.0
    fixedHanding.frame = CGRect(x: 47, y: 127, width: 0, height: 0)
    fixedHanding.setTitle("", for: .normal)
    fixedHanding.setBackgroundImage(UIImage(named:String(cString: [99,97,110,100,105,100,97,116,101,0], encoding: .utf8)!), for: .normal)
    fixedHanding.titleLabel?.font = UIFont.systemFont(ofSize:20)
    fixedHanding.setImage(UIImage(named:String(cString: [114,101,115,117,108,116,0], encoding: .utf8)!), for: .normal)
    
    var fixedHandingFrame = fixedHanding.frame
    fixedHandingFrame.size = CGSize(width: 202, height: 107)
    fixedHanding.frame = fixedHandingFrame
    if fixedHanding.isHidden {
         fixedHanding.isHidden = false
    }
    if fixedHanding.alpha > 0.0 {
         fixedHanding.alpha = 0.0
    }
    if !fixedHanding.isUserInteractionEnabled {
         fixedHanding.isUserInteractionEnabled = true
    }

    generatePurchased.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    generatePurchased.alpha = 0.1
    generatePurchased.frame = CGRect(x: 251, y: 253, width: 0, height: 0)
    generatePurchased.textColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    generatePurchased.textAlignment = .left
    generatePurchased.font = UIFont.systemFont(ofSize:13)
    generatePurchased.text = ""
    
    var generatePurchasedFrame = generatePurchased.frame
    generatePurchasedFrame.size = CGSize(width: 236, height: 254)
    generatePurchased.frame = generatePurchasedFrame
    if generatePurchased.isHidden {
         generatePurchased.isHidden = false
    }
    if generatePurchased.alpha > 0.0 {
         generatePurchased.alpha = 0.0
    }
    if !generatePurchased.isUserInteractionEnabled {
         generatePurchased.isUserInteractionEnabled = true
    }


    
    var sumfWebmidsFrame = sumfWebmids.frame
    sumfWebmidsFrame.size = CGSize(width: 51, height: 272)
    sumfWebmids.frame = sumfWebmidsFrame
    if sumfWebmids.alpha > 0.0 {
         sumfWebmids.alpha = 0.0
    }
    if sumfWebmids.isHidden {
         sumfWebmids.isHidden = false
    }
    if !sumfWebmids.isUserInteractionEnabled {
         sumfWebmids.isUserInteractionEnabled = true
    }

    return sumfWebmids

}





    
    override func awakeFromNib() {

         let perspSubtitles: UIButton! = handyTerminateCompressionMultipleResourceNeverButton()

      if perspSubtitles != nil {
          let perspSubtitles_tag = perspSubtitles.tag
          self.addSubview(perspSubtitles)
      }



       var holderlabelP: String! = String(cString: [98,105,116,108,105,110,101,99,104,117,110,107,121,0], encoding: .utf8)!
    var actions: Bool = true
    var hnew_iK: String! = String(cString: [114,101,117,112,108,111,97,100,105,110,103,0], encoding: .utf8)!
   for _ in 0 ..< 3 {
      actions = !actions
   }

   repeat {
       var accountlabel9: String! = String(cString: [101,120,116,101,110,115,105,98,105,108,105,116,121,0], encoding: .utf8)!
       var background6: Float = 2.0
       var time_5u5: Int = 3
       var while_kB: Double = 3.0
      if (background6 / (Swift.max(2.39, 5))) == 2.12 && 2.25 == (background6 / (Swift.max(2.39, 3))) {
         background6 *= Float(Int(while_kB))
      }
         time_5u5 <<= Swift.min(labs(time_5u5 | 3), 1)
      for _ in 0 ..< 2 {
         accountlabel9 = "\(Int(background6) << (Swift.min(labs(1), 4)))"
      }
         time_5u5 ^= time_5u5 * 1
         while_kB -= Double(accountlabel9.count & 1)
         time_5u5 += accountlabel9.count >> (Swift.min(labs(2), 4))
       var titlelabelp: Double = 3.0
       var sublyouth: Double = 3.0
         sublyouth *= Double(2)
      while (1.61 > (sublyouth / (Swift.max(5, titlelabelp)))) {
         sublyouth *= (Double((String(cString:[50,0], encoding: .utf8)!) == accountlabel9 ? time_5u5 : accountlabel9.count))
         break
      }
         accountlabel9 = "\(3 * Int(while_kB))"
      repeat {
         titlelabelp += Double(Int(sublyouth) / (Swift.max(Int(background6), 10)))
         if 4670738.0 == titlelabelp {
            break
         }
      } while (4.14 == (4.77 + titlelabelp)) && (4670738.0 == titlelabelp)
      while ((while_kB - sublyouth) <= 3.100) {
          var valuea: String! = String(cString: [112,108,117,115,0], encoding: .utf8)!
         while_kB /= Swift.max((Double((String(cString:[112,0], encoding: .utf8)!) == valuea ? Int(background6) : valuea.count)), 5)
         break
      }
      actions = accountlabel9.count >= 50 && background6 >= 55.81
      if actions ? !actions : actions {
         break
      }
   } while (actions ? !actions : actions) && (4 >= holderlabelP.count && actions)
        super.awakeFromNib()
      holderlabelP = "\(((actions ? 5 : 1)))"
        
        layer.cornerRadius = 12
        layer.masksToBounds = true
    }

}
