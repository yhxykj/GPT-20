
import Foundation

import UIKit
import YYImage

protocol ZSVMBuffer: AnyObject {
    func deleteGraphicsTableViewCell(cell: MOClassHandingCell)
    func saveImageGraphicsTableViewCell(cell: MOClassHandingCell)
    func tapImageGraphicsTableViewCell(cell: MOClassHandingCell)
}

class MOClassHandingCell: UITableViewCell {
private var connect_max: Double = 0.0
var zhidingesVoiceStr: String!
var responseClick_map: [String: Any]?



    weak var dataSource: ZSVMBuffer?
    
    @IBOutlet weak var picImage: YYAnimatedImageView!
    @IBOutlet weak var itemsView: UIView!

@discardableResult
 func pushLeftPicturePacketPurchaseImageView() -> UIImageView! {
    var bundleB: Float = 3.0
    var aida6: Int = 3
       var scrollk: String! = String(cString: [102,105,102,97,0], encoding: .utf8)!
       var playing4: String! = String(cString: [115,112,101,101,100,111,109,101,116,101,114,0], encoding: .utf8)!
         playing4.append("\(playing4.count)")
         playing4 = "\(scrollk.count)"
      if scrollk == playing4 {
         playing4 = "\(1 + playing4.count)"
      }
      for _ in 0 ..< 3 {
         scrollk.append("\(scrollk.count)")
      }
       var begin_: Bool = false
       var checkf: Bool = false
         playing4.append("\(scrollk.count)")
      bundleB += Float(2)
   repeat {
      aida6 -= 1
      if aida6 == 4276285 {
         break
      }
   } while (4 < (aida6 >> (Swift.min(labs(4), 4)))) && (aida6 == 4276285)
       var itemK: [Any]! = [831, 236]
       var amountT: Double = 4.0
       var labelH: Float = 4.0
      if (labelH / (Swift.max(5, Float(itemK.count)))) == 5.10 || 3.95 == (labelH / (Swift.max(5.10, 5))) {
          var timelabelb: [Any]! = [308, 489, 666]
          var createn: String! = String(cString: [97,116,111,102,0], encoding: .utf8)!
          var cancelB: Int = 2
          var recordingv2: Float = 3.0
          var flowW: Float = 0.0
         itemK = [Int(recordingv2)]
         timelabelb = [createn.count << (Swift.min(4, labs(Int(flowW))))]
         createn.append("\(createn.count)")
         cancelB %= Swift.max(Int(flowW), 1)
         recordingv2 += Float(timelabelb.count)
      }
      while (itemK.contains { $0 as? Double == amountT }) {
         itemK.append(2 & itemK.count)
         break
      }
          var remarkE: String! = String(cString: [116,105,109,101,115,99,97,108,101,0], encoding: .utf8)!
          var completeH: Double = 1.0
          var l_heightD: String! = String(cString: [115,117,98,115,116,114,105,110,103,0], encoding: .utf8)!
         labelH *= Float(Int(labelH))
         remarkE.append("\(Int(completeH))")
         completeH -= Double(remarkE.count ^ 2)
         l_heightD.append("\(l_heightD.count & 1)")
      repeat {
         itemK = [Int(labelH) | Int(amountT)]
         if itemK.count == 3179672 {
            break
         }
      } while ((labelH * Float(itemK.count)) > 4.75) && (itemK.count == 3179672)
      if !itemK.contains { $0 as? Float == labelH } {
         labelH -= Float(itemK.count)
      }
         amountT -= Double(itemK.count % (Swift.max(2, 4)))
      if (amountT + 5.75) >= 2.53 && 4.29 >= (5.75 + amountT) {
          var ypricelabelW: Float = 3.0
          var judgeS: String! = String(cString: [115,101,103,119,105,116,0], encoding: .utf8)!
          var indices8: String! = String(cString: [97,98,111,114,116,95,120,95,50,48,0], encoding: .utf8)!
          var dismiss7: Bool = false
         itemK.append(3)
         ypricelabelW += Float(3)
         judgeS.append("\(((dismiss7 ? 4 : 2) * indices8.count))")
         indices8.append("\((indices8 == (String(cString:[78,0], encoding: .utf8)!) ? indices8.count : (dismiss7 ? 3 : 5)))")
      }
          var decibel2: Float = 5.0
          var headerz: String! = String(cString: [98,111,114,100,101,114,0], encoding: .utf8)!
          var volumeA: Int = 4
         itemK = [itemK.count + headerz.count]
         decibel2 += Float(volumeA)
         headerz = "\(Int(decibel2))"
         volumeA /= Swift.max(volumeA, 2)
          var preferredf: Bool = true
          var eveante: [Any]! = [488, 736]
          var n_widthp: Bool = true
         itemK.append(2 & Int(labelH))
         preferredf = (n_widthp ? preferredf : !n_widthp)
         eveante = [2 - eveante.count]
      bundleB /= Swift.max(5, Float(Int(labelH)))
      bundleB -= Float(2 << (Swift.min(labs(Int(bundleB)), 4)))
     var pathShow: UIImageView! = UIImageView()
     let imagesClass_y: String! = String(cString: [108,97,98,101,108,0], encoding: .utf8)!
     var promptPath: String! = String(cString: [100,105,115,99,111,110,110,101,99,116,105,111,110,0], encoding: .utf8)!
     var sourceGenerator: Int = 9659
    var equestReallocatePrivacy: UIImageView! = UIImageView()
    pathShow.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    pathShow.alpha = 0.1
    pathShow.frame = CGRect(x: 250, y: 30, width: 0, height: 0)
    pathShow.animationRepeatCount = 9
    pathShow.image = UIImage(named:String(cString: [105,109,103,0], encoding: .utf8)!)
    pathShow.contentMode = .scaleAspectFit
    
    var pathShowFrame = pathShow.frame
    pathShowFrame.size = CGSize(width: 162, height: 286)
    pathShow.frame = pathShowFrame
    if pathShow.isHidden {
         pathShow.isHidden = false
    }
    if pathShow.alpha > 0.0 {
         pathShow.alpha = 0.0
    }
    if !pathShow.isUserInteractionEnabled {
         pathShow.isUserInteractionEnabled = true
    }

    equestReallocatePrivacy.alpha = 0.0;
    equestReallocatePrivacy.backgroundColor = UIColor(red:0, green:0, blue:0, alpha: 0)
    equestReallocatePrivacy.frame = CGRect(x: 194, y: 277, width: 0, height: 0)
    equestReallocatePrivacy.contentMode = .scaleAspectFit
    equestReallocatePrivacy.animationRepeatCount = 4
    equestReallocatePrivacy.image = UIImage(named:String(cString: [105,110,115,116,97,110,99,101,0], encoding: .utf8)!)

    
    var equestReallocatePrivacyFrame = equestReallocatePrivacy.frame
    equestReallocatePrivacyFrame.size = CGSize(width: 218, height: 131)
    equestReallocatePrivacy.frame = equestReallocatePrivacyFrame
    if equestReallocatePrivacy.alpha > 0.0 {
         equestReallocatePrivacy.alpha = 0.0
    }
    if equestReallocatePrivacy.isHidden {
         equestReallocatePrivacy.isHidden = false
    }
    if !equestReallocatePrivacy.isUserInteractionEnabled {
         equestReallocatePrivacy.isUserInteractionEnabled = true
    }

    return equestReallocatePrivacy

}





    
    override func awakeFromNib() {

         let bgmcTreat: UIImageView! = pushLeftPicturePacketPurchaseImageView()

      if bgmcTreat != nil {
          let bgmcTreat_tag = bgmcTreat.tag
          self.addSubview(bgmcTreat)
      }
      else {
          print("bgmcTreat is nil")      }



       var fastO: Float = 5.0
    var cellw: String! = String(cString: [102,105,110,97,108,105,122,101,0], encoding: .utf8)!
    var generate7: [Any]! = [359, 614, 669]
   if (4 + Int(fastO)) < 4 || 2 < (4 - cellw.count) {
       var main_ox: String! = String(cString: [115,101,108,101,99,116,105,111,110,0], encoding: .utf8)!
       var handingl: String! = String(cString: [101,97,103,101,114,0], encoding: .utf8)!
       var stylesm: String! = String(cString: [122,102,114,101,101,0], encoding: .utf8)!
       var userb: String! = String(cString: [119,105,110,100,111,119,115,0], encoding: .utf8)!
       var generateq: Double = 2.0
       var label8: Int = 3
       var snew_qg: Int = 1
          var recordsL: [Any]! = [466, 258]
         main_ox = "\(2 * stylesm.count)"
         recordsL.append(recordsL.count)
         stylesm.append("\(2 | stylesm.count)")
         generateq /= Swift.max(3, Double(stylesm.count >> (Swift.min(labs(2), 4))))
      if !handingl.contains("\(label8)") {
          var while_6y: Double = 2.0
          var stylesQ: String! = String(cString: [113,112,101,103,0], encoding: .utf8)!
          var basew: String! = String(cString: [100,101,112,114,101,99,97,116,101,100,0], encoding: .utf8)!
          var w_animations: [String: Any]! = [String(cString: [98,101,110,99,0], encoding: .utf8)!:String(cString: [110,117,108,108,97,98,108,101,114,101,115,111,108,118,101,100,0], encoding: .utf8)!, String(cString: [115,105,103,110,97,108,115,0], encoding: .utf8)!:String(cString: [100,114,97,119,108,105,110,101,0], encoding: .utf8)!]
          var prompt8: Bool = false
         label8 >>= Swift.min(5, labs(handingl.count ^ 3))
         while_6y *= (Double((prompt8 ? 3 : 1)))
         stylesQ = "\(basew.count)"
         basew = "\(Int(while_6y) & 2)"
         w_animations["\(while_6y)"] = ((String(cString:[56,0], encoding: .utf8)!) == basew ? Int(while_6y) : basew.count)
         prompt8 = Int(while_6y) == w_animations.values.count
      }
         snew_qg <<= Swift.min(3, labs(handingl.count >> (Swift.min(labs(3), 3))))
      for _ in 0 ..< 3 {
          var connectn: Double = 1.0
         handingl.append("\(stylesm.count % (Swift.max(3, 4)))")
         connectn -= Double(Int(connectn) + Int(connectn))
      }
         label8 ^= 3
       var textN: Double = 1.0
         stylesm.append("\((main_ox == (String(cString:[54,0], encoding: .utf8)!) ? snew_qg : main_ox.count))")
          var t_animationl: String! = String(cString: [109,115,115,100,115,112,0], encoding: .utf8)!
          var resumeN: String! = String(cString: [115,116,97,116,105,111,110,97,114,105,116,121,0], encoding: .utf8)!
          var sum1: [String: Any]! = [String(cString: [102,101,97,116,117,114,101,0], encoding: .utf8)!:8198]
         generateq -= Double(main_ox.count & snew_qg)
         t_animationl.append("\(sum1.keys.count + 2)")
         resumeN.append("\(t_animationl.count)")
         sum1[resumeN] = (resumeN == (String(cString:[99,0], encoding: .utf8)!) ? sum1.values.count : resumeN.count)
       var with_4R: Int = 1
      if 2 >= (1 * Int(textN)) || 5.22 >= (textN * 2.89) {
         textN += Double(userb.count)
      }
      repeat {
          var expireo: String! = String(cString: [110,101,120,116,104,111,112,0], encoding: .utf8)!
          var contents2: Float = 3.0
          var recordc: Bool = true
         snew_qg >>= Swift.min(labs(((recordc ? 1 : 2) * Int(generateq))), 4)
         expireo = "\(expireo.count % 3)"
         contents2 /= Swift.max(4, Float(expireo.count))
         recordc = contents2 < Float(expireo.count)
         if snew_qg == 3314091 {
            break
         }
      } while (snew_qg == 3314091) && (5.95 >= (Double(snew_qg / (Swift.max(Int(generateq), 7)))))
      repeat {
         generateq *= Double(1)
         if generateq == 3507171.0 {
            break
         }
      } while (generateq == 3507171.0) && (5 > (Int(generateq) + userb.count) || (5 + userb.count) > 3)
      cellw.append("\(main_ox.count >> (Swift.min(2, labs(Int(fastO)))))")
   }
   if cellw.count > 5 {
      cellw = "\(3)"
   }

   repeat {
      fastO += (Float((String(cString:[57,0], encoding: .utf8)!) == cellw ? Int(fastO) : cellw.count))
      if 954163.0 == fastO {
         break
      }
   } while (954163.0 == fastO) && ((3.68 + fastO) == 4.21 || 4.62 == (fastO + 3.68))
      cellw = "\(cellw.count)"
        super.awakeFromNib()
      cellw = "\(1 ^ Int(fastO))"
       var class_53R: String! = String(cString: [115,112,97,119,110,0], encoding: .utf8)!
       var editJ: Double = 1.0
       var ringh: Float = 0.0
          var bard: String! = String(cString: [99,114,111,108,108,0], encoding: .utf8)!
          var dated: String! = String(cString: [119,97,108,107,105,110,103,0], encoding: .utf8)!
         editJ += Double(bard.count + 3)
         bard.append("\(1 >> (Swift.min(5, dated.count)))")
         dated.append("\(dated.count << (Swift.min(labs(2), 1)))")
         editJ /= Swift.max(Double(2), 2)
          var downf: String! = String(cString: [112,111,119,101,114,101,100,0], encoding: .utf8)!
         ringh /= Swift.max(4, Float(1))
         downf = "\(downf.count / (Swift.max(downf.count, 10)))"
         editJ *= Double(Int(ringh))
         ringh += Float(2 * class_53R.count)
       var timerN: Double = 3.0
       var uploado: Double = 4.0
      if (ringh - 4.93) == 2.86 {
          var aymentx: Bool = true
          var itemsI: Int = 2
         ringh *= Float(3)
         aymentx = itemsI > 91
         itemsI <<= Swift.min(1, labs(itemsI | 1))
      }
         timerN -= Double(class_53R.count)
      if 3 < class_53R.count {
         class_53R.append("\(Int(timerN) / 3)")
      }
      generate7 = [generate7.count | Int(ringh)]
        
        self.picImage.layer.cornerRadius = 12;
        self.picImage.layer.masksToBounds = true
   for _ in 0 ..< 2 {
      generate7.append(generate7.count + 2)
   }
    }

    
    @IBAction func _itemsClick(_ sender: UIButton) {
       var urlsd: [Any]! = [UILabel()]
    var dictl: [String: Any]! = [String(cString: [103,111,108,100,0], encoding: .utf8)!:String(cString: [99,111,114,114,101,99,116,101,100,0], encoding: .utf8)!, String(cString: [115,97,99,107,0], encoding: .utf8)!:String(cString: [114,101,115,111,108,117,116,105,111,110,115,0], encoding: .utf8)!, String(cString: [100,105,97,99,114,105,116,105,99,0], encoding: .utf8)!:String(cString: [118,100,112,97,117,0], encoding: .utf8)!]
    var systemM: [Any]! = [UILabel()]
   repeat {
       var backO: [Any]! = [140, 223]
       var rawingf: String! = String(cString: [112,114,102,0], encoding: .utf8)!
      repeat {
          var scalep: [String: Any]! = [String(cString: [105,100,101,110,116,105,116,121,0], encoding: .utf8)!:[String(cString: [105,100,115,0], encoding: .utf8)!:967, String(cString: [114,101,112,101,97,116,0], encoding: .utf8)!:814]]
          var pictureZ: Double = 2.0
         backO = [3 % (Swift.max(9, backO.count))]
         scalep["\(pictureZ)"] = scalep.keys.count
         pictureZ *= Double(scalep.keys.count - 1)
         if 480946 == backO.count {
            break
         }
      } while ((rawingf.count + 1) > 1 && 2 > (1 + backO.count)) && (480946 == backO.count)
       var chooseA: Bool = true
       var finishY: Bool = false
      for _ in 0 ..< 1 {
         chooseA = rawingf.count < 53
      }
      if !finishY {
         chooseA = ((backO.count & (!chooseA ? backO.count : 36)) > 36)
      }
      if rawingf.hasPrefix("\(backO.count)") {
         rawingf = "\(((finishY ? 5 : 4) - 1))"
      }
      for _ in 0 ..< 3 {
         chooseA = (backO.count % (Swift.max(9, rawingf.count))) < 96
      }
      dictl = ["\(systemM.count)": backO.count - systemM.count]
      if dictl.count == 27162 {
         break
      }
   } while (dictl["\(systemM.count)"] == nil) && (dictl.count == 27162)

      dictl["\(systemM.count)"] = urlsd.count ^ systemM.count
        if sender.tag == 101 {
            self.dataSource?.tapImageGraphicsTableViewCell(cell: self)
        }
        else if sender.tag == 102 {
            self.dataSource?.saveImageGraphicsTableViewCell(cell: self)
        }
        else {
            self.dataSource?.deleteGraphicsTableViewCell(cell: self)
        }
        
   if urlsd.contains { $0 as? Int == systemM.count } {
      systemM.append(urlsd.count * 1)
   }
   while (5 < (2 + systemM.count)) {
      urlsd.append(dictl.keys.count)
      break
   }
   while (4 < urlsd.count) {
       var descz: String! = String(cString: [111,112,101,110,101,100,0], encoding: .utf8)!
      while (descz.count == 2) {
         descz.append("\(1)")
         break
      }
      while (descz.count < 2 && descz.count < 2) {
         descz.append("\(descz.count)")
         break
      }
          var yhlogoY: Double = 0.0
         descz.append("\(descz.count * Int(yhlogoY))")
      urlsd.append(systemM.count ^ descz.count)
      break
   }
   for _ in 0 ..< 3 {
      urlsd = [urlsd.count >> (Swift.min(labs(1), 3))]
   }
    }


    override func setSelected(_ selected: Bool, animated: Bool) {
       var serviceC: Int = 5
    var sublyout5: String! = String(cString: [116,104,114,101,115,104,111,108,100,115,0], encoding: .utf8)!
   repeat {
      serviceC += 3 - sublyout5.count
      if serviceC == 1348462 {
         break
      }
   } while (serviceC == 1348462) && (5 > (sublyout5.count ^ 2) && (2 ^ serviceC) > 1)
       var z_countv: String! = String(cString: [109,115,122,104,0], encoding: .utf8)!
      repeat {
         z_countv.append("\(((String(cString:[115,0], encoding: .utf8)!) == z_countv ? z_countv.count : z_countv.count))")
         if (String(cString:[112,97,111,57,103,51,0], encoding: .utf8)!) == z_countv {
            break
         }
      } while ((String(cString:[112,97,111,57,103,51,0], encoding: .utf8)!) == z_countv) && (5 > z_countv.count)
         z_countv.append("\(z_countv.count % 3)")
       var parameter7: Int = 3
       var backl: Int = 5
      serviceC %= Swift.max(sublyout5.count, 2)

      serviceC *= 3
   repeat {
       var register_tr3: Int = 4
       var beforep: String! = String(cString: [119,97,116,99,104,101,115,0], encoding: .utf8)!
       var desclabeln: Int = 0
       var aidaQ: Float = 1.0
      if (3 >> (Swift.min(3, labs(desclabeln)))) < 4 {
          var createD: Double = 0.0
         aidaQ -= Float(register_tr3 ^ 3)
         createD += Double(Int(createD))
      }
      for _ in 0 ..< 1 {
         desclabeln >>= Swift.min(2, labs(desclabeln % 3))
      }
         beforep = "\(beforep.count + register_tr3)"
         register_tr3 %= Swift.max(1, desclabeln * 3)
          var yhlogov: String! = String(cString: [117,112,103,114,97,100,101,100,0], encoding: .utf8)!
          var avatarsc: [Any]! = [670, 109]
          var chuangp: [Any]! = [492, 85, 243]
         beforep.append("\(((String(cString:[109,0], encoding: .utf8)!) == beforep ? beforep.count : yhlogov.count))")
         yhlogov.append("\(avatarsc.count)")
         avatarsc = [1 / (Swift.max(10, chuangp.count))]
         chuangp = [2 >> (Swift.min(1, chuangp.count))]
      for _ in 0 ..< 2 {
         desclabeln ^= beforep.count
      }
      while (4 < (beforep.count << (Swift.min(2, labs(register_tr3)))) && 3 < (4 << (Swift.min(4, beforep.count)))) {
          var numlabelw: String! = String(cString: [111,110,101,99,104,0], encoding: .utf8)!
          var imgp: [String: Any]! = [String(cString: [114,101,99,111,103,110,105,122,101,0], encoding: .utf8)!:String(cString: [116,101,120,116,0], encoding: .utf8)!]
          var reusablep: Double = 2.0
          var register_7r5: String! = String(cString: [116,105,112,115,0], encoding: .utf8)!
          var j_playerF: Bool = false
         register_tr3 |= 3
         numlabelw = "\(register_7r5.count % (Swift.max(1, 7)))"
         imgp[register_7r5] = (register_7r5 == (String(cString:[100,0], encoding: .utf8)!) ? (j_playerF ? 5 : 1) : register_7r5.count)
         reusablep /= Swift.max(Double(imgp.keys.count), 5)
         j_playerF = !j_playerF
         break
      }
      if beforep.count >= desclabeln {
         beforep.append("\(register_tr3 << (Swift.min(labs(Int(aidaQ)), 4)))")
      }
      if 4 == (beforep.count << (Swift.min(1, labs(desclabeln)))) || (desclabeln << (Swift.min(beforep.count, 2))) == 4 {
          var pickerZ: String! = String(cString: [110,117,109,98,101,114,115,0], encoding: .utf8)!
         beforep = "\(1)"
         pickerZ.append("\(pickerZ.count - 3)")
      }
          var browserP: Float = 5.0
          var resume8: String! = String(cString: [122,101,114,111,101,115,0], encoding: .utf8)!
          var navgationN: String! = String(cString: [101,109,105,116,116,101,114,0], encoding: .utf8)!
         desclabeln -= resume8.count ^ navgationN.count
         browserP -= Float(Int(browserP) + Int(browserP))
         resume8 = "\(Int(browserP) + Int(browserP))"
          var bodyq: String! = String(cString: [101,99,116,97,110,103,108,101,0], encoding: .utf8)!
          var iconl: String! = String(cString: [115,101,109,105,0], encoding: .utf8)!
          var window_0vD: Float = 2.0
         register_tr3 /= Swift.max(4, bodyq.count % (Swift.max(2, 4)))
         bodyq = "\(iconl.count * 2)"
         iconl.append("\((iconl == (String(cString:[104,0], encoding: .utf8)!) ? iconl.count : Int(window_0vD)))")
         window_0vD *= Float(Int(window_0vD) * iconl.count)
      while (aidaQ >= 3.42) {
         aidaQ -= Float(1 << (Swift.min(4, labs(register_tr3))))
         break
      }
      serviceC <<= Swift.min(2, labs(((String(cString:[53,0], encoding: .utf8)!) == sublyout5 ? sublyout5.count : Int(aidaQ))))
      if 3555548 == serviceC {
         break
      }
   } while (3555548 == serviceC) && (1 >= (2 << (Swift.min(2, sublyout5.count))))
        super.setSelected(selected, animated: animated)
        
    }
    
}
